name: CI - Registry Cleanup (GHCR)

on:
    schedule:
        - cron: '0 4 * * 0'
    workflow_dispatch:
        inputs:
            dry_run:
                description: Preview deletions without executing them
                type: boolean
                default: true

permissions:
    packages: write
    issues: write

concurrency:
    group: registry-cleanup-ghcr
    cancel-in-progress: false

jobs:
    collect:
        if: github.repository == 'KBVE/kbve'
        runs-on: ubuntu-latest
        outputs:
            image_names: ${{ steps.list.outputs.image_names }}
        steps:
            - uses: actions/checkout@v7

            - name: Setup Node v24
              uses: actions/setup-node@v7
              with:
                  node-version: 24

            - name: Setup moon
              uses: ./.github/actions/setup-moon

            # Read off the project graph rather than from a hand-maintained
            # list. Pruning is destructive, and the failure mode of a stale list
            # is deleting an image the workspace still builds.
            - id: list
              run: |
                  NAMES=$(node tools/release/docker-images.mjs)
                  echo "Resolved images: ${NAMES}"
                  echo "image_names=${NAMES}" >> "$GITHUB_OUTPUT"

    prune:
        needs: collect
        runs-on: ubuntu-latest
        steps:
            - uses: snok/container-retention-policy@v3.1.0
              with:
                  account: kbve
                  token: ${{ secrets.UNITY_PAT }}
                  image-names: ${{ needs.collect.outputs.image_names }}
                  image-tags: '!latest !*.*.*'
                  tag-selection: both
                  cut-off: 1w
                  keep-n-most-recent: 20
                  dry-run: ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && 'true' || 'false' }}

    notify:
        needs: [collect, prune]
        if: failure() && github.event_name == 'schedule' && github.repository == 'KBVE/kbve'
        runs-on: ubuntu-latest
        steps:
            - uses: actions/github-script@v9
              with:
                  script: |
                      const label = 'registry-cleanup';
                      const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
                      const title = 'Weekly GHCR registry cleanup failed';
                      const body = [
                        `The scheduled GHCR image cleanup run failed.`,
                        ``,
                        `- Run: ${runUrl}`,
                        `- Commit: ${context.sha}`,
                        ``,
                        `Check the \`collect\` / \`prune\` job logs. Common causes: \`UNITY_PAT\` missing \`delete:packages\` scope, GHCR API rate limit, or a project graph that resolved no images.`,
                      ].join('\n');
                      const existing = await github.rest.issues.listForRepo({
                        owner: context.repo.owner,
                        repo: context.repo.repo,
                        state: 'open',
                        labels: label,
                      });
                      if (existing.data.length > 0) {
                        await github.rest.issues.createComment({
                          owner: context.repo.owner,
                          repo: context.repo.repo,
                          issue_number: existing.data[0].number,
                          body,
                        });
                      } else {
                        await github.rest.issues.create({
                          owner: context.repo.owner,
                          repo: context.repo.repo,
                          title,
                          body,
                          labels: [label],
                        });
                      }
