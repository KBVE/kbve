name: CI Chuck Launcher

on:
    workflow_dispatch:

permissions:
    contents: read

jobs:
    build:
        uses: ./.github/workflows/utils-tauri-build.yml
        with:
            project: chuck-launcher
            project_path: apps/chuckrpg/chuck-launcher
            app_name: chuck-launcher
            notarize: true
        secrets: inherit
