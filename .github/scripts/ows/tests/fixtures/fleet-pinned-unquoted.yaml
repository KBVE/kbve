apiVersion: agones.dev/v1
kind: Fleet
metadata:
    name: rows-chuckrpg-beta
spec:
    template:
        metadata:
            labels:
                ows.kbve.com/server-version: 0.3.61
        spec:
            template:
                spec:
                    containers:
                        - name: ue5-server
                          env:
                              - name: OWS_SERVER_VERSION
                                value: 0.3.61
                        - name: build-reporter
                          env:
                              - name: OWS_SERVER_VERSION
                                value: 0.3.61
