
[workspace]
resolver = '2'
members = [
	'apps/rust_api_profile',
	'packages/kbve',
	'packages/erust',
	'apps/rust_wasm_embed',
]

[profile.release]
opt-level = 'z'
lto = true
strip = true
codegen-units = 1
panic = 'abort'
