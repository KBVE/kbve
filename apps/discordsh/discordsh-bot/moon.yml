$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'rust'
tags: ['rust', 'docker']

tasks:
    containerx:
        script: 'docker buildx build --load -f apps/discordsh/discordsh-bot/Dockerfile -t kbve/discordsh-bot:latest .'
        options:
            runFromWorkspaceRoot: true
            cache: false
