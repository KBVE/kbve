[package]
name = "herbmail-server"
authors = ["kbve", "h0lybyte"]
version = "0.0.1"
edition = "2024"
rust-version.workspace = true
license = "MIT"
description = "Herbmail game server — authoritative sim for the herbmail dungeon crawler."

[lib]
name = "herbmail_server"
path = "src/lib.rs"

[dependencies]
