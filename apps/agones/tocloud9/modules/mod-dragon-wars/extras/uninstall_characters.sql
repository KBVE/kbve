-- Sortie log removal. Run against acore_characters.
-- Dropping this discards the record of every squadron ever launched. The
-- planes themselves are temporary summons and leave nothing behind.

DROP TABLE IF EXISTS `mod_dragon_wars_sorties`;
