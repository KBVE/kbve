[project]
name = "kbve-orc-tools"
version = "0.0.0"
description = "Dev tooling for the kbve-orc Factorio mod (static validation + sprite baking). Not published."
requires-python = ">=3.12,<3.14"
dependencies = [
    "pillow>=12.0,<13.0",
]
