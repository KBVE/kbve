{
  "name": "irc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/irc",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx e2e irc-gateway"],
        "parallel": false
      }
    },
    "container": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx container irc-gateway"],
        "parallel": false
      }
    }
  },
  "tags": []
}
