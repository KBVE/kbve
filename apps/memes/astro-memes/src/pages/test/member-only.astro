---
import Layouts from "../../layouts/core/Layouts.astro";
import MemberOnlyComponent from '../../components/ui/MemberOnly.astro';
import MemberOnlyDashboardComponent from '../../components/ui/MemberOnlyDashboard.astro';
import MemberOnlyPremiumComponent from '../../components/ui/MemberOnlyPremium.astro';
---

<Layouts title="Member Only Test - Meme.sh" Onboarding={false}>
    <div class="min-h-screen bg-gradient-to-br from-zinc-950 via-zinc-900 to-emerald-950/20 py-8">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto space-y-8">
                <!-- Header -->
                <div class="text-center">
                    <h1 class="text-4xl font-bold bg-gradient-to-r from-emerald-400 to-green-400 bg-clip-text text-transparent mb-4">
                        Member-Only Access Test
                    </h1>
                    <p class="text-neutral-400 max-w-2xl mx-auto">
                        This page demonstrates the member-only access control functionality. 
                        The content below should only be visible to authenticated users.
                    </p>
                </div>

                <!-- Public Content -->
                <div class="bg-zinc-800/50 border border-zinc-700/50 rounded-lg p-6">
                    <h2 class="text-2xl font-semibold text-white mb-4">🌐 Public Content</h2>
                    <p class="text-neutral-300 mb-4">
                        This content is visible to everyone, regardless of authentication status.
                    </p>
                    <div class="bg-green-500/10 border border-green-500/20 rounded-lg p-4">
                        <p class="text-green-400 text-sm">
                            ✅ You can see this content because it's public.
                        </p>
                    </div>
                </div>

                <!-- Basic Member-Only Content with Custom Features -->
                <MemberOnlyComponent 
                    message="Please sign in to access exclusive member features."
                    memberContent={{
                        title: "🔒 Exclusive Member Content",
                        description: "Congratulations! You can see this content because you're authenticated.",
                        features: [
                            "Advanced meme creation tools",
                            "Priority upload queue", 
                            "Custom meme templates",
                            "Analytics dashboard",
                            "Member-only discussions",
                            "Early access to features"
                        ]
                    }}
                />

                <!-- Member Dashboard Demo -->
                <MemberOnlyDashboardComponent message="Sign in to access your personal dashboard." />

                <!-- Premium Features Demo -->
                <MemberOnlyPremiumComponent message="Join our premium community for exclusive features!" />

                <!-- Simple Member-Only Content without OAuth -->
                <MemberOnlyComponent 
                    message="Simple login required for basic features." 
                    showOAuth={false}
                    memberContent={{
                        title: "⚡ Quick Access Content",
                        description: "This content uses simple login/register buttons instead of OAuth.",
                        features: [
                            "Basic meme creation",
                            "Public galleries access",
                            "Standard templates",
                            "Community participation"
                        ]
                    }}
                />

                <!-- Test Instructions -->
                <div class="bg-zinc-800/50 border border-zinc-700/50 rounded-lg p-6">
                    <h3 class="text-xl font-semibold text-white mb-4">🧪 Testing Instructions</h3>
                    <ol class="space-y-2 text-neutral-300 text-sm">
                        <li><strong>1. Without Authentication:</strong> You should see login/register prompts for all protected sections</li>
                        <li><strong>2. After Login/Register:</strong> Protected content should become visible with different themes</li>
                        <li><strong>3. Component Types:</strong> Notice the different member-only component styles:
                            <ul class="ml-4 mt-1 space-y-1">
                                <li>• Basic member content (emerald theme)</li>
                                <li>• Dashboard content (blue/purple theme)</li>
                                <li>• Premium features (yellow/orange theme)</li>
                                <li>• Simple OAuth-free content</li>
                            </ul>
                        </li>
                        <li><strong>4. Self-Contained:</strong> All components are fully self-contained with no slot dependencies</li>
                        <li><strong>5. Session Persistence:</strong> Refresh the page - your authentication should persist</li>
                        <li><strong>6. Logout Test:</strong> Log out and verify content becomes protected again</li>
                    </ol>
                    
                    <div class="mt-4 p-4 bg-emerald-500/10 border border-emerald-500/20 rounded-lg">
                        <p class="text-emerald-400 text-sm">
                            ✅ <strong>SSG Compatible:</strong> These components are fully compatible with Astro's static site generation 
                            and will work correctly in production builds.
                        </p>
                    </div>
                </div>

                <!-- Navigation -->
                <div class="flex flex-wrap gap-4 justify-center">
                    <a 
                        href="/profile" 
                        class="px-6 py-3 bg-gradient-to-r from-emerald-500 to-green-500 hover:from-emerald-400 hover:to-green-400 text-white font-semibold rounded-xl transition-all duration-300 hover:scale-105"
                    >
                        View Profile
                    </a>
                    <a 
                        href="/onboarding" 
                        class="px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-500 hover:from-blue-400 hover:to-purple-400 text-white font-semibold rounded-xl transition-all duration-300 hover:scale-105"
                    >
                        Onboarding
                    </a>
                    <a 
                        href="/auth/login" 
                        class="px-6 py-3 bg-zinc-700 hover:bg-zinc-600 text-white font-semibold rounded-xl transition-all duration-300 hover:scale-105"
                    >
                        Login Page
                    </a>
                    <a 
                        href="/auth/register" 
                        class="px-6 py-3 bg-zinc-700 hover:bg-zinc-600 text-white font-semibold rounded-xl transition-all duration-300 hover:scale-105"
                    >
                        Register Page
                    </a>
                </div>
            </div>
        </div>
    </div>
</Layouts>
