$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'typescript'

tasks:
    e2e:
        deps:
            [
                'herbmail-api:test',
                'herbmail-api:container',
                'herbmail-api-e2e:e2e-docker',
            ]
        options:
            cache: false
    container:
        deps: ['herbmail-api:container']
        options:
            cache: false
