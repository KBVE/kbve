apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: firecracker-rootfs
    namespace: kilobase
    labels:
        app: firecracker-ctl
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 2Gi
