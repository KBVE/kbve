apiVersion: v1
kind: ResourceQuota
metadata:
    name: firecracker-quota
    namespace: firecracker
    labels:
        app: firecracker-ctl
spec:
    hard:
        pods: '10'
        requests.cpu: '4'
        requests.memory: 4Gi
        limits.cpu: '8'
        limits.memory: 8Gi
