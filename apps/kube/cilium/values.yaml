## Cilium Helm values for Talos Linux (v1.8.0+)
## Reference: https://docs.siderolabs.com/kubernetes-guides/cni/deploying-cilium

# CNI — allow Multus to coexist with Cilium
cni:
    exclusive: false

# Device — pin Cilium to the external-facing interface.
# Without this, Cilium auto-detects flannel.1 (legacy).
devices:
    - enp195s0

# NodePort direct routing device — must match the external-facing interface.
nodePort:
    directRoutingDevice: enp195s0

ipam:
    mode: kubernetes

# kube-proxy replacement — ENABLED. Phase 1 CNI swap completed 2026-03-19.
# Talos machine config: cluster.proxy.disabled=true, cni.name=none.
kubeProxyReplacement: true
k8sServiceHost: localhost # KubePrism (already enabled on port 7445)
k8sServicePort: 7445

# Talos cgroup v2 — do not auto-mount, use host path
cgroup:
    autoMount:
        enabled: false
    hostRoot: /sys/fs/cgroup

# Talos security — SYS_MODULE not allowed, must be explicitly excluded
securityContext:
    capabilities:
        ciliumAgent:
            - CHOWN
            - KILL
            - NET_ADMIN
            - NET_RAW
            - IPC_LOCK
            - SYS_ADMIN
            - SYS_RESOURCE
            - DAC_OVERRIDE
            - FOWNER
            - SETGID
            - SETUID
        cleanCiliumState:
            - NET_ADMIN
            - SYS_ADMIN
            - SYS_RESOURCE

# WireGuard encryption — pod-to-pod + node-to-node
encryption:
    enabled: true
    type: wireguard
    nodeEncryption: true

# Observability — Hubble DISABLED due to nil pointer crash in observer.
# Re-enable after upgrading to a Cilium version with the fix.
hubble:
    enabled: false
    relay:
        enabled: false
    ui:
        enabled: false

# Gateway API — LB service mode (working on 1.18.0)
gatewayAPI:
    enabled: true
    enableAlpn: true
    enableAppProtocol: true

# L2 announcements — replaces MetalLB
# Includes ^br.* for bridge interface support
l2announcements:
    enabled: true

externalIPs:
    enabled: true

operator:
    replicas: 1
