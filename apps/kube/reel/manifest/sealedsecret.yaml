---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
    name: reel-api
    namespace: reel
spec:
    encryptedData:
        REEL_API_TOKEN: REPLACE_WITH_KUBESEAL_OUTPUT
    template:
        metadata:
            name: reel-api
            namespace: reel
