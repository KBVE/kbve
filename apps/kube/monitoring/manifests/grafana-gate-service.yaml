apiVersion: v1
kind: Service
metadata:
    name: grafana-gate
    namespace: monitoring
    labels:
        app: grafana-gate
spec:
    type: ClusterIP
    selector:
        app: grafana-gate
    ports:
        - name: http
          port: 5678
          targetPort: 5678
          protocol: TCP
