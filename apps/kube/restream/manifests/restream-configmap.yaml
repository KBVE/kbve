apiVersion: v1
kind: ConfigMap
metadata:
  name: restreamer-config
  namespace: restream
  labels:
    app: restreamer
data:
  # Basic configuration for restreamer
  # These can be customized based on specific needs
  log_level: "info"
  # Additional environment variables can be added here
  # and mounted as environment variables in the deployment