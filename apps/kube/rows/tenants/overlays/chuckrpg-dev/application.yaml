apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: rows-chuckrpg-dev
    namespace: argocd
    finalizers:
        - resources-finalizer.argocd.argoproj.io
spec:
    project: default
    source:
        repoURL: https://github.com/KBVE/kbve
        targetRevision: main
        path: apps/kube/rows/tenants/overlays/chuckrpg-dev
    destination:
        server: https://kubernetes.default.svc
        namespace: rows-chuckrpg-dev
    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - CreateNamespace=true
            - PrunePropagationPolicy=foreground
            - PruneLast=true
        retry:
            limit: 5
            backoff:
                duration: 5s
                factor: 2
                maxDuration: 3m
    revisionHistoryLimit: 3
