apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: chuckrpg-api-tls
    namespace: rows
spec:
    secretName: chuckrpg-api-tls
    issuerRef:
        name: letsencrypt-http
        kind: ClusterIssuer
    dnsNames:
        - api.chuckrpg.com
