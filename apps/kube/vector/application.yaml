apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: vector
    namespace: argocd
    annotations:
        kbve.com/source-path: kube/vector
        kbve.com/manifest-path: apps/kube/vector/manifests
        kbve.com/application-file: kube/vector/application.yaml
        kbve.com/managed-by: argocd
        kbve.com/schema-version: v1
        kbve.com/category: observability
        kbve.com/stack: core
spec:
    project: default
    source:
        repoURL: https://github.com/kbve/kbve
        targetRevision: main
        path: apps/kube/vector/manifests
    destination:
        server: https://kubernetes.default.svc
        namespace: vector
    syncPolicy:
        automated:
            selfHeal: true
            prune: true
        syncOptions:
            - CreateNamespace=true
            - ServerSideApply=true
            - Replace=true
            - Force=true
            - RespectIgnoreDifferences=true
        retry:
            limit: 3
            backoff:
                duration: 5s
                factor: 2
                maxDuration: 3m
