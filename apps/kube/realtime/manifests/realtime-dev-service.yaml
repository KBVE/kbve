apiVersion: v1
kind: Service
metadata:
  name: realtime-dev
  namespace: kilobase
  labels:
    app: realtime
    component: websocket
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: realtime