apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kilobase

resources:
  - realtime-statefulset.yaml
  - realtime-service.yaml
  - realtime-headless-service.yaml
  - realtime-jwt-external-secret.yaml

configMapGenerator:
  - name: realtime-seeds
    files:
      - seeds.exs