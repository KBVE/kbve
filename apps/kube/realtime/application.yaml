apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: realtime
    namespace: argocd
    finalizers:
        - resources-finalizer.argocd.argoproj.io
    annotations:
        kbve.com/source-path: kube/realtime
        kbve.com/manifest-path: apps/kube/realtime/manifests
        kbve.com/application-file: kube/realtime/application.yaml
        kbve.com/managed-by: argocd
        kbve.com/schema-version: v1
        kbve.com/category: application
        kbve.com/stack: supabase
spec:
    project: default
    source:
        repoURL: https://github.com/kbve/kbve
        targetRevision: HEAD
        path: apps/kube/realtime/manifests
    destination:
        server: https://kubernetes.default.svc
        namespace: kilobase
    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - CreateNamespace=false
            - ServerSideApply=true
            - RespectIgnoreDifferences=true
            - Replace=true
