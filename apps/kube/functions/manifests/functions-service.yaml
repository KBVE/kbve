apiVersion: v1
kind: Service
metadata:
  name: functions
  namespace: kilobase
spec:
  type: ClusterIP
  selector:
    app: functions
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: edge-runtime