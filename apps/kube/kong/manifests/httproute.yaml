apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
    name: kilobase-api-route
    namespace: kilobase
spec:
    parentRefs:
        - name: kbve-gateway
          namespace: kbve
    hostnames:
        - api.kbve.com
        - supabase.kbve.com
    rules:
        - matches:
              - path:
                    type: PathPrefix
                    value: /
          backendRefs:
              - name: kong-kong-proxy
                port: 80
