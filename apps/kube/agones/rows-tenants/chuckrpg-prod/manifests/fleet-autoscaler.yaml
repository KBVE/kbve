apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
    name: rows-chuckrpg-prod-autoscaler
    namespace: arc-runners
    labels:
        app: ows-gameserver
        app.kubernetes.io/part-of: ows
        app.kubernetes.io/instance: chuckrpg-prod
spec:
    fleetName: rows-chuckrpg-prod
    policy:
        type: Buffer
        buffer:
            bufferSize: 1
            minReplicas: 0
            maxReplicas: 10
    sync:
        type: FixedInterval
        fixedInterval:
            seconds: 30
