apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: agones-friendslop
    namespace: argocd
    labels:
        app.kubernetes.io/part-of: friendslop
    annotations:
        kbve.com/source-path: 'apps/kube/agones/friendslop'
        kbve.com/manifest-path: 'apps/kube/agones/friendslop/manifests'
        kbve.com/application-file: 'apps/kube/agones/friendslop/application.yaml'
        kbve.com/managed-by: 'argocd'
        kbve.com/schema-version: 'v1'
        kbve.com/category: 'game-server'
        kbve.com/stack: 'agones'
spec:
    project: default
    source:
        repoURL: https://github.com/kbve/kbve
        targetRevision: main
        path: apps/kube/agones/friendslop/manifests
    destination:
        server: https://kubernetes.default.svc
        namespace: friendslop
    ignoreDifferences:
        # The FleetAutoscaler owns replicas; syncing them back would fight it.
        - group: agones.dev
          kind: Fleet
          jsonPointers:
              - /spec/replicas
    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - CreateNamespace=true
            - ServerSideApply=true
            - RespectIgnoreDifferences=true
