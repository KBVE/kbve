apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: factorio-saves
    namespace: factorio
    labels:
        app.kubernetes.io/part-of: factorio
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 10Gi
