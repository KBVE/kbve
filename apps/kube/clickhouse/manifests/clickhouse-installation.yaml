apiVersion: clickhouse.altinity.com/v1
kind: ClickHouseInstallation
metadata:
    name: clickhouse-cluster
    namespace: clickhouse
spec:
    configuration:
        users:
            admin/password_sha256_hex: 'c0cf946ad8414c46a5977371703b1ed1bcaaddeb477355ba66f9572b4d28226d'
            admin/networks/ip:
                - '::/0'
            admin/profile: 'default'
            admin/access_management: 1
        # Top-level ZooKeeper configuration - references the keeper service
        zookeeper:
            nodes:
                - host: keeper-clickhouse-keeper.clickhouse.svc.cluster.local
                  port: 2181
        files:
            config.d/zzz-postgres.xml: |
                <clickhouse>
                  <named_collections>
                    <supabase_postgres>
                      <host>supabase-cluster-rw.kilobase.svc.cluster.local</host>
                      <port>5432</port>
                      <database>supabase</database>
                      <user from_env="CLICKHOUSE_POSTGRES_USER"/>
                      <password from_env="CLICKHOUSE_POSTGRES_PASSWORD"/>
                    </supabase_postgres>
                  </named_collections>
                </clickhouse>
        clusters:
            - name: cluster
              layout:
                  shardsCount: 2
                  replicasCount: 2
    defaults:
        templates:
            dataVolumeClaimTemplate: data-storage
            logVolumeClaimTemplate: log-storage
            podTemplate: clickhouse-with-postgres
    templates:
        podTemplates:
            - name: clickhouse-with-postgres
              spec:
                  containers:
                      - name: clickhouse
                        image: clickhouse/clickhouse-server:25.8.4
                        env:
                            - name: CLICKHOUSE_POSTGRES_USER
                              valueFrom:
                                  secretKeyRef:
                                      name: clickhouse-postgres-credentials
                                      key: username
                            - name: CLICKHOUSE_POSTGRES_PASSWORD
                              valueFrom:
                                  secretKeyRef:
                                      name: clickhouse-postgres-credentials
                                      key: password
                            - name: PGSSLMODE
                              value: 'disable'
                            - name: PGSSLCERT
                              value: ''
                            - name: PGSSLKEY
                              value: ''
                        volumeMounts:
                            - name: clickhouse-postgres-credentials
                              mountPath: /etc/clickhouse-server/secrets/
                              readOnly: true
                  volumes:
                      - name: clickhouse-postgres-credentials
                        secret:
                            secretName: clickhouse-postgres-credentials
        volumeClaimTemplates:
            - name: data-storage
              spec:
                  accessModes:
                      - ReadWriteOnce
                  resources:
                      requests:
                          storage: 50Gi
                  storageClassName: longhorn
            - name: log-storage
              spec:
                  accessModes:
                      - ReadWriteOnce
                  resources:
                      requests:
                          storage: 10Gi
                  storageClassName: longhorn
