# PyDesk

FastAPI application built on the `fudster` library. Provides WebSocket broadcasting, CORS middleware, and RuneLite integration.

## Development

```bash
moon run pydesk:test
moon run pydesk:lint
moon run pydesk:build
```
