---
title: Download ChuckRPG
description: Live download links for ChuckRPG desktop clients (Windows, macOS, Linux), resolved from itch.io.
---

import DownloadList from '../../../components/game/DownloadList.astro';

Desktop clients are published to itch.io and served through the ChuckRPG backend.
The links below resolve to the latest **live** build per platform — a build still
processing on itch shows as pending until it finishes.

<DownloadList />

The full live page is at [chuckrpg.com/downloads](/downloads), rendered server-side
by the backend so it always reflects the current itch.io build state.
