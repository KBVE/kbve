---
// AskamaForumThreadProvider — first-class thread page layout.
// Served by axum-kbve at /forum/t/{slug_or_id}.
//
// Askama placeholders consumed by axum-kbve's ForumThreadTemplate:
//   {{ thread_title }}
//   {{ thread_slug_or_id }}
//   {{ space_slug }}, {{ space_name }}
//   {{ author_username }}, {{ author_avatar_url }}
//   {{ created_at_human }}, {{ last_activity_human }}
//   {{ score }}, {{ comment_count }}
//   {{ thread_body_html }}     — sanitized markdown HTML (kbve::markdown)
//   {{ thread_meta_description }}
//   {{ comments_html }}        — server-composed comment tree
//   {{ tags_html }}            — server-composed tag chips (may be empty)
//
// thread_body_html, comments_html, tags_html arrive sanitized via
// `kbve::markdown::render` + `forum_render` partials. Pass them through
// `set:html`. Plain-text fields auto-escape via askama default.
---

<div class="forum-shell">
	<nav class="forum-shell__breadcrumb" aria-label="Breadcrumb">
		<a href="/forum/">Forum</a>
		<span aria-hidden="true">›</span>
		<a href={`/forum/s/{{ space_slug }}`}>{`{{ space_name }}`}</a>
		<span aria-hidden="true">›</span>
		<span class="forum-shell__breadcrumb-current">
			{`{{ thread_title }}`}
		</span>
	</nav>

	<div class="forum-shell__layout">
		<main class="forum-shell__main">
			<article class="thread-card">
				<header class="thread-card__header">
					<h1 class="thread-card__title">{`{{ thread_title }}`}</h1>

					<div class="thread-card__meta">
						<a
							class="thread-card__author"
							href={`/@{{ author_username }}`}>
							<span
								class="thread-card__author-avatar"
								data-avatar={`{{ author_avatar_url }}`}
								data-letter={`{{ author_username }}`}>
							</span>
							<span>@{`{{ author_username }}`}</span>
						</a>
						<span aria-hidden="true">·</span>
						<time>{`{{ created_at_human }}`}</time>
						<span aria-hidden="true">·</span>
						<span class="thread-card__last-active">
							active {`{{ last_activity_human }}`}
						</span>
					</div>

					<div class="thread-card__chips">
						<span class="thread-card__chip">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="14"
								height="14"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true">
								<path d="m5 12 7-7 7 7"></path><path
									d="M12 19V5">
								</path>
							</svg>
							{`{{ score }}`} pts
						</span>
						<span class="thread-card__chip">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="14"
								height="14"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true">
								<path d="M7.9 20A9 9 0 1 0 4 16.1L2 22z"></path>
							</svg>
							{`{{ comment_count }}`} replies
						</span>
					</div>

					<div
						class="thread-card__tags"
						set:html={`{{ tags_html|safe }}`}
					/>
				</header>

				<div
					class="thread-card__body"
					set:html={`{{ thread_body_html|safe }}`}
				/>
			</article>

			<section class="thread-comments" aria-labelledby="comments-heading">
				<header class="thread-comments__header">
					<h2 id="comments-heading" class="thread-comments__title">
						Comments
						<span class="thread-comments__count">
							{`{{ comment_count }}`}
						</span>
					</h2>
				</header>

				<form
					id="thread-compose-form"
					class="thread-compose"
					method="post"
					action={`/api/v1/forum/t/{{ thread_slug_or_id }}/comments`}
					data-thread-slug={`{{ thread_slug_or_id }}`}
					novalidate>
					<label
						for="thread-compose-body"
						class="thread-compose__label">
						Reply with markdown
					</label>
					<textarea
						id="thread-compose-body"
						class="thread-compose__textarea"
						name="body"
						rows="4"
						placeholder="Be civil. Markdown supported."
						minlength="1"
						maxlength="20000"
						required>
					</textarea>
					<div class="thread-compose__row">
						<p class="thread-compose__hint">
							Need a username first? Set one from your <a
								href="/dashboard/profile/">
								profile
							</a>.
						</p>
						<button class="thread-compose__submit" type="submit">
							Post reply
						</button>
					</div>
					<output
						id="thread-compose-status"
						class="thread-compose__status">
					</output>
				</form>

				<div
					class="thread-comments__tree"
					set:html={`{{ comments_html|safe }}`}
				/>
			</section>
		</main>

		<aside class="forum-shell__sidebar" aria-label="Thread sidebar">
			<section class="forum-card">
				<h2 class="forum-card__title">Space</h2>
				<a class="forum-card__space" href={`/forum/s/{{ space_slug }}`}>
					<span class="forum-card__space-name">
						{`{{ space_name }}`}
					</span>
					<span class="forum-card__space-slug">
						s/{`{{ space_slug }}`}
					</span>
				</a>
				<a class="forum-card__cta" href={`/forum/s/{{ space_slug }}`}>
					Browse threads →
				</a>
			</section>

			<section class="forum-card">
				<h2 class="forum-card__title">Thread actions</h2>
				<ul class="forum-card__actions">
					<li>
						<button data-action="bookmark" type="button" disabled>
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="14"
								height="14"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true">
								<path
									d="m19 21-7-4-7 4V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v16z">
								</path>
							</svg>
							Bookmark
						</button>
					</li>
					<li>
						<button data-action="subscribe" type="button" disabled>
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="14"
								height="14"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true">
								<path d="M10.268 21a2 2 0 0 0 3.464 0">
								</path><path
									d="M3.262 15.326A1 1 0 0 0 4 17h16a1 1 0 0 0 .74-1.673C19.41 13.956 18 12.499 18 8A6 6 0 0 0 6 8c0 4.499-1.411 5.956-2.738 7.326">
								</path>
							</svg>
							Subscribe
						</button>
					</li>
					<li>
						<button data-action="report" type="button" disabled>
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="14"
								height="14"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true">
								<path
									d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z">
								</path><line x1="4" x2="4" y1="22" y2="15">
								</line>
							</svg>
							Report
						</button>
					</li>
				</ul>
				<p
					class="forum-card__copy"
					data-thread-actions-hint
					data-anon-text="Sign in to use these actions."
					data-auth-text="These actions are coming soon.">
					Sign in to use these actions.
				</p>
			</section>
		</aside>
	</div>

	<dialog
		id="forum-mod-dialog"
		class="forum-mod-dialog"
		aria-labelledby="forum-mod-dialog-title">
		<form class="forum-mod-dialog__form" method="dialog">
			<header class="forum-mod-dialog__header">
				<h2 id="forum-mod-dialog-title" class="forum-mod-dialog__title">
					Edit comment
				</h2>
				<button
					type="button"
					class="forum-mod-dialog__close"
					data-action="cancel"
					aria-label="Close dialog">
					×
				</button>
			</header>
			<p class="forum-mod-dialog__warning" hidden></p>
			<label class="forum-mod-dialog__field" data-field="body">
				<span class="forum-mod-dialog__label">Body (markdown)</span>
				<textarea
					name="body"
					rows="6"
					maxlength="20000"
					class="forum-mod-dialog__textarea">
				</textarea>
			</label>
			<label class="forum-mod-dialog__field" data-field="reason">
				<span class="forum-mod-dialog__label">
					Reason <span class="forum-mod-dialog__hint">
						(optional)
					</span>
				</span>
				<textarea
					name="reason"
					rows="2"
					maxlength="500"
					class="forum-mod-dialog__textarea"
					placeholder="Visible in the moderation log.">
				</textarea>
			</label>
			<footer class="forum-mod-dialog__footer">
				<button
					type="button"
					class="forum-mod-dialog__btn"
					data-action="cancel">
					Cancel
				</button>
				<button
					type="submit"
					class="forum-mod-dialog__btn forum-mod-dialog__btn--primary"
					data-action="confirm">
					Save
				</button>
			</footer>
		</form>
	</dialog>
</div>

<script>
	import { initSupa, getSupa } from '@/lib/supa';
	import { addToast } from '@kbve/droid';
	import { onMount } from '@kbve/astro/utils/clientRouter';

	onMount(() => {
		// prettier-plugin-astro keeps reformatting <textarea ... /> back
		// into <textarea>...\n  </textarea>, which means the textarea's
		// initial value is leading whitespace and the cursor opens
		// indented. Force-clear every textarea on load.
		for (const t of document.querySelectorAll<HTMLTextAreaElement>(
			'.thread-compose__textarea, .forum-mod-dialog__textarea',
		)) {
			if (!t.defaultValue || /^\s+$/.test(t.defaultValue)) {
				t.value = '';
				t.defaultValue = '';
			}
		}

		// Initial-letter fallback for the author avatar slot. The askama
		// template emits `data-avatar="<url-or-empty>"` and `data-letter="<username>"`.
		// When the URL is non-empty we swap the letter for an `<img>`; otherwise
		// the first character renders inside the rounded square.
		for (const el of document.querySelectorAll<HTMLElement>(
			'.thread-card__author-avatar',
		)) {
			const url = el.dataset.avatar?.trim();
			const letter =
				el.dataset.letter?.trim().charAt(0).toUpperCase() || '?';
			if (url) {
				const img = document.createElement('img');
				img.src = url;
				img.alt = '';
				img.width = 32;
				img.height = 32;
				el.appendChild(img);
			} else {
				el.textContent = letter;
			}
		}

		// Swap the Thread Actions hint based on auth state. The buttons
		// stay disabled until bookmark/subscribe/report endpoints land —
		// this just stops telling signed-in users to sign in.
		(async () => {
			const hint = document.querySelector<HTMLElement>(
				'[data-thread-actions-hint]',
			);
			if (!hint) return;
			try {
				await initSupa();
			} catch {
				return;
			}
			const supa = getSupa();
			const sessionResult = await supa.getSession().catch(() => null);
			const isAuthed = !!sessionResult?.session?.access_token;
			const next = isAuthed
				? hint.dataset.authText
				: hint.dataset.anonText;
			if (next) hint.textContent = next;
		})();

		// Comment compose: submit via fetch with the local Supabase access
		// token. axum's POST /api/v1/forum/t/{slug}/comments runs the
		// service_create_comment RPC and returns the new comment id.
		const composeForm = document.getElementById(
			'thread-compose-form',
		) as HTMLFormElement | null;
		const composeStatus = document.getElementById(
			'thread-compose-status',
		) as HTMLOutputElement | null;

		function setComposeStatus(
			kind: 'idle' | 'busy' | 'error' | 'ok',
			msg = '',
		) {
			if (!composeStatus) return;
			composeStatus.dataset.kind = kind;
			composeStatus.textContent = msg;
		}

		composeForm?.addEventListener('submit', async (ev) => {
			ev.preventDefault();
			setComposeStatus('busy', 'Posting…');

			try {
				await initSupa();
			} catch {
				setComposeStatus(
					'error',
					'Could not initialize auth. Reload and try again.',
				);
				return;
			}
			const supa = getSupa();
			// SupabaseGateway returns `{ session, ... }` — unwrap before
			// reaching for access_token. Without this we read undefined and
			// every signed-in user sees "Sign in first to comment."
			const sessionResult = await supa.getSession().catch(() => null);
			const session = sessionResult?.session ?? null;
			if (!session?.access_token) {
				setComposeStatus('error', 'Sign in first to comment.');
				return;
			}

			const slug = composeForm.dataset.threadSlug || '';
			const fd = new FormData(composeForm);
			const payload = { body: String(fd.get('body') || '') };

			try {
				const res = await fetch(`/api/v1/forum/t/${slug}/comments`, {
					method: 'POST',
					headers: {
						'Content-Type': 'application/json',
						Authorization: `Bearer ${session.access_token}`,
					},
					body: JSON.stringify(payload),
				});
				if (!res.ok) {
					const body = await res.text().catch(() => '');
					setComposeStatus(
						'error',
						`${res.status}: ${body || res.statusText}`,
					);
					return;
				}
				setComposeStatus('ok', 'Posted! Reloading…');
				window.location.reload();
			} catch (err) {
				setComposeStatus(
					'error',
					err instanceof Error ? err.message : 'Network error',
				);
			}
		});

		// ── Comment row actions: edit-own / mod-edit / mod-remove ─────
		(async () => {
			const rows = Array.from(
				document.querySelectorAll<HTMLElement>(
					'.forum-comment[data-comment-id]',
				),
			);
			if (rows.length === 0) return;

			try {
				await initSupa();
			} catch {
				return;
			}
			const supa = getSupa();
			const sessionResult = await supa.getSession().catch(() => null);
			const session = sessionResult?.session ?? null;
			if (!session?.access_token) return;

			const myId = String(session.user?.id ?? '');
			let isStaff = false;
			try {
				const res = await fetch('/api/v1/me/staff', {
					headers: {
						Authorization: `Bearer ${session.access_token}`,
					},
				});
				if (res.ok) {
					const data = (await res.json()) as { is_staff?: boolean };
					isStaff = !!data.is_staff;
				}
			} catch {
				/* swallow — we just hide the mod buttons */
			}

			for (const row of rows) {
				const authorId = row.dataset.authorId || '';
				const menuEl = row.querySelector<HTMLDetailsElement>(
					'.forum-comment__menu',
				);
				if (!menuEl) continue;

				const isOwn = myId && authorId === myId;
				const editOwnBtn = menuEl.querySelector<HTMLButtonElement>(
					'[data-action="edit-own"]',
				);
				const modEditBtn = menuEl.querySelector<HTMLButtonElement>(
					'[data-action="mod-edit"]',
				);
				const modRemoveBtn = menuEl.querySelector<HTMLButtonElement>(
					'[data-action="mod-remove"]',
				);

				if (editOwnBtn) editOwnBtn.hidden = !isOwn;
				if (modEditBtn) modEditBtn.hidden = !isStaff;
				if (modRemoveBtn) modRemoveBtn.hidden = !isStaff;

				if (isOwn || isStaff) menuEl.hidden = false;
			}

			// One <dialog> shared by all rows; mode + currentBody are passed
			// per-open. Using HTMLDialogElement keeps focus trapping + ESC
			// cancel free, no library required. Submission is gated on the
			// dialog's `close` event, so the form's `method="dialog"` natively
			// closes with returnValue="confirm" on submit.
			type ModMode = 'edit-own' | 'mod-edit' | 'mod-remove';
			type ModResult = { body?: string; reason?: string } | null;

			const modDialog = document.getElementById(
				'forum-mod-dialog',
			) as HTMLDialogElement | null;
			const modDialogTitle =
				modDialog?.querySelector<HTMLElement>(
					'.forum-mod-dialog__title',
				) ?? null;
			const modDialogWarn =
				modDialog?.querySelector<HTMLElement>(
					'.forum-mod-dialog__warning',
				) ?? null;
			const modBodyField =
				modDialog?.querySelector<HTMLElement>('[data-field="body"]') ??
				null;
			const modReasonField =
				modDialog?.querySelector<HTMLElement>(
					'[data-field="reason"]',
				) ?? null;
			const modBodyInput =
				modDialog?.querySelector<HTMLTextAreaElement>(
					'textarea[name="body"]',
				) ?? null;
			const modReasonInput =
				modDialog?.querySelector<HTMLTextAreaElement>(
					'textarea[name="reason"]',
				) ?? null;
			const modPrimaryBtn =
				modDialog?.querySelector<HTMLButtonElement>(
					'.forum-mod-dialog__btn--primary',
				) ?? null;
			const modCancelBtns = modDialog
				? modDialog.querySelectorAll<HTMLButtonElement>(
						'[data-action="cancel"]',
					)
				: null;

			let pendingResolve: ((r: ModResult) => void) | null = null;
			let pendingMode: ModMode | null = null;

			modCancelBtns?.forEach((btn) =>
				btn.addEventListener('click', () => modDialog?.close('cancel')),
			);

			modDialog?.addEventListener('close', () => {
				if (!pendingResolve) return;
				const action = modDialog.returnValue;
				const mode = pendingMode;
				const showBody = mode !== 'mod-remove';
				const showReason = mode !== 'edit-own';
				const resolver = pendingResolve;
				pendingResolve = null;
				pendingMode = null;
				if (action !== 'confirm') {
					resolver(null);
					return;
				}
				resolver({
					body: showBody ? (modBodyInput?.value ?? '') : undefined,
					reason: showReason
						? (modReasonInput?.value ?? '')
						: undefined,
				});
			});

			function openModDialog(
				mode: ModMode,
				currentBody: string,
			): Promise<ModResult> {
				if (!modDialog) return Promise.resolve(null);
				const isRemove = mode === 'mod-remove';
				const isModEdit = mode === 'mod-edit';
				pendingMode = mode;
				if (modDialogTitle) {
					modDialogTitle.textContent = isRemove
						? 'Remove comment'
						: isModEdit
							? 'Mod-edit comment'
							: 'Edit your comment';
				}
				if (modDialogWarn) {
					if (isRemove) {
						modDialogWarn.textContent =
							"This decrements the thread's comment count and cannot be undone.";
						modDialogWarn.hidden = false;
					} else {
						modDialogWarn.hidden = true;
						modDialogWarn.textContent = '';
					}
				}
				if (modBodyField) modBodyField.hidden = isRemove;
				if (modReasonField) modReasonField.hidden = mode === 'edit-own';
				if (modBodyInput) {
					modBodyInput.value = isRemove ? '' : currentBody;
					modBodyInput.required = !isRemove;
				}
				if (modReasonInput) modReasonInput.value = '';
				if (modPrimaryBtn) {
					modPrimaryBtn.textContent = isRemove ? 'Remove' : 'Save';
					modPrimaryBtn.classList.toggle(
						'forum-mod-dialog__btn--danger',
						isRemove,
					);
				}
				modDialog.showModal();
				(isRemove ? modReasonInput : modBodyInput)?.focus();
				return new Promise((resolve) => {
					pendingResolve = resolve;
				});
			}

			document.addEventListener('click', async (ev) => {
				const target = ev.target as HTMLElement | null;
				if (!target) return;
				const btn = target.closest<HTMLButtonElement>(
					'.forum-comment__action',
				);
				if (!btn) return;

				const row = btn.closest<HTMLElement>(
					'.forum-comment[data-comment-id]',
				);
				if (!row) return;
				const commentId = row.dataset.commentId || '';
				const action = btn.dataset.action as ModMode | undefined;
				if (!commentId || !action) return;

				ev.preventDefault();

				// Close the kebab menu so the dialog has visual focus.
				const menuEl = row.querySelector<HTMLDetailsElement>(
					'.forum-comment__menu',
				);
				if (menuEl) menuEl.open = false;

				const supa2 = getSupa();
				const sr = await supa2.getSession().catch(() => null);
				const sess = sr?.session ?? null;
				if (!sess?.access_token) {
					addToast({
						id: `forum-mod-auth-${Date.now()}`,
						message: 'Sign in first to moderate or edit.',
						severity: 'warning',
						duration: 4000,
					});
					return;
				}

				const rawTpl = row.querySelector<HTMLTemplateElement>(
					'.forum-comment__raw',
				);
				const currentBody = rawTpl?.innerHTML?.trim() ?? '';

				const result = await openModDialog(action, currentBody);
				if (!result) return;

				let url = '';
				let method = 'PATCH';
				let body: Record<string, string> | null = null;

				if (action === 'edit-own') {
					const next = (result.body ?? '').trim();
					if (!next || next === currentBody) return;
					url = `/api/v1/forum/c/${commentId}`;
					body = { body: next };
				} else if (action === 'mod-edit') {
					const next = (result.body ?? '').trim();
					if (!next || next === currentBody) return;
					url = `/api/v1/forum/c/${commentId}/moderate`;
					body = { body: next, reason: result.reason ?? '' };
				} else if (action === 'mod-remove') {
					url = `/api/v1/forum/c/${commentId}`;
					method = 'DELETE';
					body = { body: '', reason: result.reason ?? '' };
				} else {
					return;
				}

				btn.disabled = true;
				try {
					const res = await fetch(url, {
						method,
						headers: {
							'Content-Type': 'application/json',
							Authorization: `Bearer ${sess.access_token}`,
						},
						body: JSON.stringify(body),
					});
					if (!res.ok) {
						const text = await res.text().catch(() => '');
						addToast({
							id: `forum-mod-err-${Date.now()}`,
							message: `${res.status}: ${text || res.statusText}`,
							severity: 'error',
							duration: 6000,
						});
						btn.disabled = false;
						return;
					}
					addToast({
						id: `forum-mod-ok-${Date.now()}`,
						message:
							action === 'mod-remove'
								? 'Comment removed.'
								: 'Comment updated.',
						severity: 'success',
						duration: 3000,
					});
					window.location.reload();
				} catch (err) {
					addToast({
						id: `forum-mod-net-${Date.now()}`,
						message:
							err instanceof Error
								? err.message
								: 'Network error',
						severity: 'error',
						duration: 6000,
					});
					btn.disabled = false;
				}
			});
		})();
	});
</script>

<style>
	:root {
		--forum-accent: var(--sl-color-accent, #8b5cf6);
		--forum-accent-soft: color-mix(
			in srgb,
			var(--forum-accent) 18%,
			transparent
		);
		--forum-bg: var(--sl-color-bg, #09090b);
		--forum-bg-soft: var(--sl-color-bg-nav, #131318);
		--forum-bg-card: var(--sl-color-black, #0c0c10);
		--forum-border: var(--sl-color-gray-5, #2a2a33);
		--forum-text: var(--sl-color-text, #fafafa);
		--forum-muted: var(--sl-color-gray-2, #a1a1aa);
		--forum-radius: 12px;
		--forum-radius-sm: 8px;
		--forum-shadow: 0 1px 2px rgba(0, 0, 0, 0.18);
	}

	.forum-shell {
		max-width: 1180px;
		margin: 0 auto;
		padding: 1.5rem 1.25rem 4rem;
		color: var(--forum-text);
	}

	/* Starlight's splash template wraps content in `.sl-markdown-content`
	 * which adds its own paragraph margins, max-width, and prose styles.
	 * Reset the wrapper so our forum shell can use the full width and
	 * own the spacing. Scoped to `.forum-shell` ancestors so we don't
	 * touch the rest of Starlight. */
	:global(.sl-markdown-content):has(.forum-shell) {
		max-width: none;
		margin: 0;
	}
	:global(.sl-markdown-content) .forum-shell :is(p, h1, h2, h3, h4) {
		margin-top: 0;
	}

	.forum-shell__breadcrumb {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4rem;
		align-items: center;
		font-size: 0.8125rem;
		color: var(--forum-muted);
		margin-bottom: 1rem;
	}
	.forum-shell__breadcrumb a {
		color: var(--forum-muted);
		text-decoration: none;
	}
	.forum-shell__breadcrumb a:hover {
		color: var(--forum-accent);
	}
	.forum-shell__breadcrumb-current {
		color: var(--forum-text);
		font-weight: 500;
		max-width: 28rem;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.forum-shell__layout {
		display: grid;
		gap: 1.5rem;
		grid-template-columns: minmax(0, 1fr);
	}
	@media (min-width: 920px) {
		.forum-shell__layout {
			grid-template-columns: minmax(0, 1fr) 280px;
		}
	}

	.forum-shell__main {
		display: grid;
		gap: 1.25rem;
	}

	/* ── Thread card ──────────────────────────────────────────── */
	.thread-card {
		background: var(--forum-bg-card);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius);
		padding: 1.25rem 2.75rem 1.75rem;
	}
	@media (max-width: 640px) {
		.thread-card {
			padding: 1rem 1.5rem 1.25rem;
		}
	}
	.thread-card__header {
		display: grid;
		gap: 0.85rem;
		padding-bottom: 1.25rem;
		border-bottom: 1px solid var(--forum-border);
	}
	.thread-card__title {
		font-size: clamp(1.5rem, 2.6vw, 2.125rem);
		line-height: 1.2;
		margin: 0;
		letter-spacing: -0.01em;
	}
	.thread-card__meta {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		align-items: center;
		font-size: 0.875rem;
		color: var(--forum-muted);
	}
	.thread-card__author {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--forum-text);
		text-decoration: none;
		font-weight: 500;
	}
	.thread-card__author:hover {
		color: var(--forum-accent);
	}
	.thread-card__author-avatar {
		width: 32px;
		height: 32px;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 8px;
		background: var(--forum-accent-soft);
		color: var(--forum-text);
		font-weight: 600;
		font-size: 0.875rem;
		text-transform: uppercase;
		overflow: hidden;
	}
	.thread-card__author-avatar img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
	.thread-card__last-active {
		color: var(--forum-muted);
	}
	.thread-card__chips {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}
	.thread-card__chip {
		display: inline-flex;
		align-items: center;
		gap: 0.3rem;
		padding: 0.25rem 0.65rem;
		background: var(--forum-bg-soft);
		border: 1px solid var(--forum-border);
		border-radius: 999px;
		font-size: 0.8125rem;
		color: var(--forum-text);
	}
	.thread-card__tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem;
	}
	.thread-card__tags:empty {
		display: none;
	}
	.thread-card__tags :global(.forum-tag-chip) {
		display: inline-flex;
		align-items: center;
		padding: 0.2rem 0.6rem;
		background: var(--forum-accent-soft);
		color: var(--forum-text);
		border-radius: 999px;
		font-size: 0.75rem;
		font-weight: 500;
		text-decoration: none;
		transition: background 0.12s ease;
	}
	.thread-card__tags :global(.forum-tag-chip:hover) {
		background: var(--forum-accent);
		color: #fff;
	}

	.thread-card__body {
		padding-top: 1.5rem;
		font-size: 1rem;
		line-height: 1.7;
		color: var(--forum-text);
	}
	.thread-card__body :global(p) {
		margin: 0 0 1em;
	}
	.thread-card__body :global(p:last-child) {
		margin-bottom: 0;
	}
	/* Cap rendered-markdown headings so a one-line `# Title` does not
	   blow up to the splash template's huge default H1 and visually
	   crowd the card padding. The forum-shell margin reset zeros
	   margin-top, so we re-add explicit space above. */
	.thread-card__body :global(h1),
	.thread-card__body :global(h2),
	.thread-card__body :global(h3) {
		margin: 1.5em 0 0.5em;
		line-height: 1.3;
		font-weight: 600;
		color: var(--forum-text);
	}
	.thread-card__body :global(h1) {
		font-size: 1.625rem;
	}
	.thread-card__body :global(h2) {
		font-size: 1.375rem;
	}
	.thread-card__body :global(h3) {
		font-size: 1.125rem;
	}
	.thread-card__body :global(:first-child) {
		margin-top: 0;
	}
	.thread-card__body :global(pre) {
		overflow-x: auto;
		padding: 1rem;
		border-radius: var(--forum-radius-sm);
		background: var(--forum-bg-soft);
		font-size: 0.875rem;
	}
	.thread-card__body :global(code) {
		background: var(--forum-bg-soft);
		padding: 0.1em 0.35em;
		border-radius: 4px;
		font-size: 0.9em;
	}
	.thread-card__body :global(pre code) {
		background: transparent;
		padding: 0;
	}
	.thread-card__body :global(blockquote) {
		border-left: 3px solid var(--forum-accent);
		padding: 0.25rem 0 0.25rem 1rem;
		color: var(--forum-muted);
		margin: 1rem 0;
		background: var(--forum-bg-soft);
		border-radius: 0 var(--forum-radius-sm) var(--forum-radius-sm) 0;
	}
	.thread-card__body :global(a) {
		color: var(--forum-accent);
		text-decoration: underline;
		text-decoration-thickness: 1px;
		text-underline-offset: 2px;
	}
	.thread-card__body :global(a:hover) {
		text-decoration-thickness: 2px;
	}
	.thread-card__body :global(img) {
		max-width: 100%;
		border-radius: var(--forum-radius-sm);
	}

	/* ── Comments section ─────────────────────────────────────── */
	.thread-comments {
		background: var(--forum-bg-card);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius);
		padding: 1.5rem 2.75rem 2rem;
		display: grid;
		gap: 1.25rem;
	}
	@media (max-width: 640px) {
		.thread-comments {
			padding: 1.25rem 1.5rem 1.5rem;
		}
	}
	.thread-comments__header {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 1rem;
	}
	.thread-comments__title {
		font-size: 1.125rem;
		font-weight: 600;
		margin: 0;
	}
	.thread-comments__count {
		display: inline-flex;
		min-width: 1.75rem;
		justify-content: center;
		padding: 0.05rem 0.5rem;
		margin-left: 0.4rem;
		background: var(--forum-bg-soft);
		border-radius: 999px;
		font-size: 0.8125rem;
		font-weight: 500;
		color: var(--forum-muted);
	}

	.thread-compose {
		display: grid;
		gap: 0.7rem;
		padding: 1.25rem 1.5rem;
		background: var(--forum-bg-soft);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius);
	}
	.thread-compose__label {
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.06em;
		color: var(--forum-muted);
	}
	.thread-compose__textarea {
		width: 100%;
		resize: vertical;
		padding: 0.75rem;
		font-family: inherit;
		font-size: 0.9375rem;
		line-height: 1.5;
		background: var(--forum-bg);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius-sm);
		color: var(--forum-text);
	}
	.thread-compose__textarea:focus {
		outline: 2px solid var(--forum-accent);
		outline-offset: -2px;
	}
	.thread-compose__textarea:disabled {
		opacity: 0.6;
		cursor: not-allowed;
	}
	.thread-compose__row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 1rem;
		flex-wrap: wrap;
	}
	.thread-compose__hint {
		font-size: 0.8125rem;
		color: var(--forum-muted);
		margin: 0;
	}
	.thread-compose__hint a {
		color: var(--forum-accent);
	}
	.thread-compose__submit {
		padding: 0.5rem 1.1rem;
		background: var(--forum-accent);
		color: white;
		border: none;
		border-radius: var(--forum-radius-sm);
		font-size: 0.875rem;
		font-weight: 600;
		cursor: pointer;
		transition: opacity 0.12s ease;
	}
	.thread-compose__submit:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	.thread-compose__status {
		font-size: 0.8125rem;
		min-height: 1.25em;
	}
	.thread-compose__status[data-kind='busy'] {
		color: var(--forum-muted);
	}
	.thread-compose__status[data-kind='error'] {
		color: #f87171;
	}
	.thread-compose__status[data-kind='ok'] {
		color: #34d399;
	}

	/* ── Comment cards ────────────────────────────────────────── */
	.thread-comments__tree :global(.forum-comment) {
		padding: 1.15rem 1.5rem;
		border-left: 2px solid var(--forum-border);
		margin-left: calc(var(--depth, 0) * 1.25rem);
		background: var(--forum-bg-soft);
		border-radius: 0 var(--forum-radius-sm) var(--forum-radius-sm) 0;
		display: grid;
		gap: 0.5rem;
		margin-bottom: 0.75rem;
	}
	.thread-comments__tree :global(.forum-comment[data-depth='1']) {
		--depth: 1;
		border-left-color: var(--forum-accent-soft);
	}
	.thread-comments__tree :global(.forum-comment[data-depth='2']) {
		--depth: 2;
	}
	.thread-comments__tree :global(.forum-comment[data-depth='3']) {
		--depth: 3;
	}
	.thread-comments__tree :global(.forum-comment[data-depth='4']) {
		--depth: 4;
	}
	.thread-comments__tree :global(.forum-comment[data-depth='5']) {
		--depth: 5;
	}
	.thread-comments__tree :global(.forum-comment__header) {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4rem;
		font-size: 0.8125rem;
		color: var(--forum-muted);
	}
	.thread-comments__tree :global(.forum-comment__header a) {
		color: var(--forum-text);
		font-weight: 500;
		text-decoration: none;
	}
	.thread-comments__tree :global(.forum-comment__header a:hover) {
		color: var(--forum-accent);
	}
	.thread-comments__tree :global(.forum-comment__menu) {
		position: relative;
		margin-left: auto;
	}
	.thread-comments__tree :global(.forum-comment__menu[hidden]) {
		display: none;
	}
	.thread-comments__tree :global(.forum-comment__menu-toggle) {
		list-style: none;
		cursor: pointer;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 1.5rem;
		height: 1.5rem;
		border-radius: 6px;
		color: var(--forum-muted);
		font-size: 1rem;
		line-height: 1;
		user-select: none;
		transition:
			background 0.12s ease,
			color 0.12s ease;
	}
	.thread-comments__tree
		:global(.forum-comment__menu-toggle::-webkit-details-marker) {
		display: none;
	}
	.thread-comments__tree :global(.forum-comment__menu-toggle:hover),
	.thread-comments__tree
		:global(.forum-comment__menu[open] .forum-comment__menu-toggle) {
		background: var(--forum-bg-soft);
		color: var(--forum-text);
	}
	.thread-comments__tree :global(.forum-comment__menu-items) {
		position: absolute;
		top: calc(100% + 0.25rem);
		right: 0;
		display: flex;
		flex-direction: column;
		min-width: 9rem;
		padding: 0.25rem;
		gap: 0.1rem;
		background: var(--forum-bg-card);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius-sm);
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
		z-index: 4;
	}
	.thread-comments__tree :global(.forum-comment__action) {
		appearance: none;
		border: 0;
		background: transparent;
		color: var(--forum-muted);
		padding: 0.4rem 0.6rem;
		border-radius: 4px;
		font-size: 0.8125rem;
		font-weight: 500;
		text-align: left;
		cursor: pointer;
		transition:
			color 0.12s ease,
			background 0.12s ease;
	}
	.thread-comments__tree :global(.forum-comment__action[hidden]) {
		display: none;
	}
	.thread-comments__tree :global(.forum-comment__action:hover) {
		color: var(--forum-text);
		background: var(--forum-bg-soft);
	}
	.thread-comments__tree :global(.forum-comment__action--danger) {
		color: #f87171;
	}
	.thread-comments__tree :global(.forum-comment__action--danger:hover) {
		color: #fff;
		background: #b91c1c;
	}
	.thread-comments__tree :global(.forum-comment__action:disabled) {
		opacity: 0.5;
		cursor: not-allowed;
	}

	.forum-mod-dialog {
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius);
		background: var(--forum-bg-card);
		color: var(--forum-text);
		padding: 0;
		width: min(36rem, calc(100vw - 2rem));
		box-shadow: 0 24px 64px rgba(0, 0, 0, 0.55);
	}
	.forum-mod-dialog::backdrop {
		background: rgba(0, 0, 0, 0.6);
		backdrop-filter: blur(2px);
	}
	.forum-mod-dialog__form {
		display: flex;
		flex-direction: column;
		gap: 0.85rem;
		padding: 1.1rem 1.25rem 1.25rem;
		margin: 0;
	}
	.forum-mod-dialog__header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
	}
	.forum-mod-dialog__title {
		margin: 0;
		font-size: 1.05rem;
		font-weight: 600;
	}
	.forum-mod-dialog__close {
		appearance: none;
		border: 0;
		background: transparent;
		color: var(--forum-muted);
		font-size: 1.25rem;
		line-height: 1;
		cursor: pointer;
		padding: 0.15rem 0.5rem;
		border-radius: 6px;
	}
	.forum-mod-dialog__close:hover {
		color: var(--forum-text);
		background: var(--forum-bg-soft);
	}
	.forum-mod-dialog__warning {
		margin: 0;
		padding: 0.55rem 0.75rem;
		background: rgba(248, 113, 113, 0.1);
		border: 1px solid rgba(248, 113, 113, 0.4);
		border-radius: var(--forum-radius-sm);
		color: #fca5a5;
		font-size: 0.85rem;
	}
	.forum-mod-dialog__warning[hidden] {
		display: none;
	}
	.forum-mod-dialog__field {
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}
	.forum-mod-dialog__field[hidden] {
		display: none;
	}
	.forum-mod-dialog__label {
		font-size: 0.8rem;
		font-weight: 500;
		color: var(--forum-muted);
	}
	.forum-mod-dialog__hint {
		color: var(--forum-muted);
		font-weight: 400;
	}
	.forum-mod-dialog__textarea {
		width: 100%;
		resize: vertical;
		padding: 0.55rem 0.7rem;
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius-sm);
		background: var(--forum-bg);
		color: var(--forum-text);
		font: inherit;
		font-size: 0.9rem;
		line-height: 1.45;
	}
	.forum-mod-dialog__textarea:focus-visible {
		outline: 2px solid var(--forum-accent);
		outline-offset: 0;
		border-color: var(--forum-accent);
	}
	.forum-mod-dialog__footer {
		display: flex;
		justify-content: flex-end;
		gap: 0.5rem;
	}
	.forum-mod-dialog__btn {
		appearance: none;
		border: 1px solid var(--forum-border);
		background: transparent;
		color: var(--forum-text);
		padding: 0.4rem 0.95rem;
		border-radius: 6px;
		font-size: 0.85rem;
		font-weight: 500;
		cursor: pointer;
		transition:
			background 0.12s ease,
			border-color 0.12s ease,
			color 0.12s ease;
	}
	.forum-mod-dialog__btn:hover {
		border-color: var(--forum-accent);
	}
	.forum-mod-dialog__btn--primary {
		background: var(--forum-accent);
		color: #fff;
		border-color: var(--forum-accent);
	}
	.forum-mod-dialog__btn--primary:hover {
		filter: brightness(1.05);
	}
	.forum-mod-dialog__btn--danger {
		background: #b91c1c;
		border-color: #b91c1c;
		color: #fff;
	}
	.forum-mod-dialog__btn--danger:hover {
		filter: brightness(1.05);
	}
	.thread-comments__tree :global(.forum-comment__body) {
		font-size: 0.9375rem;
		line-height: 1.55;
		color: var(--forum-text);
	}
	.thread-comments__tree :global(.forum-comment__body p) {
		margin: 0 0 0.6em;
	}
	.thread-comments__tree :global(.forum-comment__body p:last-child) {
		margin-bottom: 0;
	}
	.thread-comments__tree :global(.forum-comment__body code) {
		background: var(--forum-bg);
		padding: 0.1em 0.35em;
		border-radius: 4px;
		font-size: 0.9em;
	}

	/* ── Sidebar ──────────────────────────────────────────────── */
	.forum-shell__sidebar {
		display: grid;
		gap: 1rem;
		align-self: start;
		position: sticky;
		top: 1rem;
	}
	.forum-card {
		background: var(--forum-bg-card);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius);
		padding: 1rem 1.25rem;
		display: grid;
		gap: 0.6rem;
	}
	.forum-card__title {
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		color: var(--forum-muted);
		margin: 0;
	}
	.forum-card__space {
		display: grid;
		gap: 0.1rem;
		text-decoration: none;
		color: var(--forum-text);
	}
	.forum-card__space-name {
		font-size: 0.9375rem;
		font-weight: 600;
	}
	.forum-card__space-slug {
		font-size: 0.75rem;
		color: var(--forum-muted);
		font-family: ui-monospace, SFMono-Regular, monospace;
	}
	.forum-card__cta {
		font-size: 0.8125rem;
		font-weight: 600;
		color: var(--forum-accent);
		text-decoration: none;
	}
	.forum-card__cta:hover {
		text-decoration: underline;
	}
	.forum-card__actions {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 0.4rem;
	}
	.forum-card__actions button {
		width: 100%;
		display: inline-flex;
		align-items: center;
		gap: 0.55rem;
		text-align: left;
		padding: 0.5rem 0.7rem;
		background: var(--forum-bg-soft);
		border: 1px solid var(--forum-border);
		border-radius: var(--forum-radius-sm);
		color: var(--forum-text);
		font-size: 0.875rem;
		cursor: pointer;
		transition:
			border-color 0.12s ease,
			background 0.12s ease;
	}
	.forum-card__actions button :global(svg) {
		flex-shrink: 0;
		color: var(--forum-muted);
	}
	.forum-card__actions button:hover:not(:disabled) {
		border-color: var(--forum-accent);
		background: var(--forum-accent-soft);
	}
	.forum-card__actions button:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}
	.forum-card__copy {
		font-size: 0.8125rem;
		color: var(--forum-muted);
		margin: 0;
	}
</style>
