import { z } from 'zod';


// TODO: Fix React Button to Add Item into Inventory

// Calling Unity from React.
// Object to Call -> KoalaMainInventory
// Script -> Inventory
// Function -> public virtual bool AddItem(InventoryItem itemToAdd, int quantity)
// Issue is the itemToAdd is an "InventoryItem" aka Scriptable Object.