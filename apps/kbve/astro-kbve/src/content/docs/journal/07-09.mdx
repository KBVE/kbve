---
title: 'July: 09'
category: Daily
date: 2025-07-09 12:00:00
client: Self
unsplash: 1718976142386-e9ebe34d3aee
img: https://images.unsplash.com/photo-1718976142386-e9ebe34d3aee?crop=entropy&cs=srgb&fm=jpg&ixid=MnwzNjM5Nzd8MHwxfHJhbmRvbXx8fHx8fHx8fDE2ODE3NDg2ODY&ixlib=rb-4.0.3&q=85
description: |
  July 9th. NPC Engine
tags:
  - daily
template: splash
tableOfContents: false
editUrl: false
lastUpdated: false
prev: false
next: false
bento:
    badge: 'journal · daily log'
    title: 'July 9,'
    accent: every year.
    lede: 'The July 9th daily log — notes, tasks, and quotes collected across every year of the KBVE journal.'
    ctas:
        - label: Read the log
          href: '#2025'
          primary: true
        - label: '← Jul 8'
          href: /journal/07-08/
        - label: 'Jul 10 →'
          href: /journal/07-10/
    related:
        - title: 'Previous · Jul 8'
          copy: 'Step back a day to the July 8th log.'
          href: /journal/07-08/
          icon: calendar
        - title: 'Next · Jul 10'
          copy: 'Jump ahead to the July 10th log.'
          href: /journal/07-10/
          icon: arrow
        - title: 'Journal hub'
          copy: 'Every daily log, grouped by month, plus the blog archive and RSS feed.'
          href: /journal/
          icon: book
    theme:
        hero_bg: 'https://images.unsplash.com/photo-1718976142386-e9ebe34d3aee?q=80&w=2400&auto=format&fit=crop'
        accent: '#a78bfa'
        accent2: '#f472b6'
        btn_fg: '#1e1035'
---

import { Adsense } from '@/components/astropad';
import BentoDoc from '@/components/hero/BentoDoc.astro';
import BentoProse from '@/components/hero/BentoProse.astro';

<BentoDoc data={frontmatter.bento}>

<BentoProse id="2025" heading="2025">

### Nvidia 4T

-	04:36PM

	Great to see the big green machine push towards that 4 trillion cap, I wonder where it will be in 40 years from now.
	I am going to try and get around a 1000 shares before the end of this year, just to further push my nest egg.
	With that being said, I been wheeling the puts, using that capital back to invest into rareicon.

### AstroPad Supabase

-	03:44PM

	In the future, I will try and add some hook systems so that the astro pad can directly hook into vite configuration.
	This will be something that we add in the future, the goal being that we make this as modular as possible.

</BentoProse>

<section class="bento-section not-content">
	<div class="bento-frame bento-band">
		<Adsense />
	</div>
</section>

<BentoProse id="2024" heading="2024">

**TSLA**

Looking at this tesla spike is really getting me excited!
The market might finally see some of the value within the `ai` and `energy` areas that the company could push forward.

**NPC Engine**

I am adding another couple layers of complexity to the NPC engine but I think the time invested now will pay off in the future.
The plan will be similar to how we moved the items out of the games core and now we can create new items through the MDX.

The schema looks like this right now:

```ts


const NPCPositionSchema = z.object({
	x: z.number(),
	y: z.number(),
	z: z.number().optional(),
	d: z.number().optional(),
  });

const PlayerStatsSchema = z.object({
	health: z.number(),
	mana: z.number(),
  });

const NPCDataSchema = z.object({
	id: z.string(),
	name: z.string(),
	spriteKey: z.string(),
	walkingAnimationMapping: z.number(),
	startPosition: NPCPositionSchema,
	speed: z.number(),
	scale: z.number(),
	actions: z.array(z.enum(['talk', 'quest', 'trade', 'combat', 'heal', 'steal'])),
	effects: z.array(z.string()).optional(),
	stats: PlayerStatsSchema.optional(),
	spriteImageId: z.string().optional(),
	avatarImageId: z.string().optional(),
  });

const SpriteSchema = z.object({
	id: z.string(), // Assuming the ULID is validated elsewhere
	spriteName: z.string(),
	assetLocation: z.string(),
	frameWidth: z.number(),
	frameHeight: z.number(),
	scale: z.number().optional(),
	spriteData: z.instanceof(Blob).optional(),
  });

const AvatarSchema = z.object({
	id: z.string(), // Assuming the ULID is validated elsewhere
	avatarLocation: z.string(),
	avatarData: z.instanceof(Blob).optional(),
});
  
  
```

I am thinking that we could split the three schemas into their own `z.objects` , then just having the `NPC` reference the sprite and the avatar.
This schema is not yet complete because we still have to do the dialogue system.

Okay the first NPC is Chip and after adding him in, we need to create the JSON endpoints to pull the data from.
We will follow the same structure as the itemdb but pivot the data points towards the avatar, sprite and then npcdb.


**DevOps**

The devops library should have internal git commands that are built into it, making it easier to branch off the codebase and make changes that would be scope specific.
I still need to work out the exact steps and logic that would be involved in getting it to work without having major problems.
Also we could expand groq by having it build out items for our game using the mdx layout.
The user would make an issue ticket for the item that they would want to add into the game, then have it parse through the mdx contents and add the variables/data points to the itemdb.
There is still some work that would go into it, hmm, maybe I am writing a bit too much and need to do some more coding.


**ItemsDB - Fish**

The next couple items to add into the `Fish.mdx` will be these:

- Nemo Fish
- Zebra Fish
- Ghost Lobster
- Warhammer Shark
- Blue Angular
- Rainforest Frog
- Koi
- Blood Octopus
- Bowhead Whale
- Seahorse
- Blue Seahorse
- Posion Seashorse
- Pink Shrimp
- White Squid
- Starfish De Patrick

These are the generic items that we can add into the category, I am just making this list so that I can remind myself to add them into the itemdb.

<Adsense />

### 2023
- 1:24am - Just finished finished a quick league match and I figured it would make sense to go through more of the backlog grooming stage.

</BentoProse>

<BentoProse id="quote" heading="Quote">

> Genuine sincerity opens people's hearts, while manipulation causes them to close.
> — <cite>Daisaku Ikeda</cite>

---

</BentoProse>

<BentoProse id="tasks" heading="Tasks">

- [ ] Sunday Funday!

</BentoProse>

</BentoDoc>
