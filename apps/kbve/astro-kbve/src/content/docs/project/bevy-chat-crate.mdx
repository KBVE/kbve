---
title: bevy_chat — IRC Chat & Event Bridge Crate
description: bevy_chat is a headless async IRC client crate for MUD and game clients, unifying native tokio TCP and WASM WebSocket transports with an optional Bevy plugin.
sem: 1
sidebar:
    label: bevy_chat
tags:
    - crates
    - rust
    - bevy
    - irc
    - chat
key: bevy_chat_crate
pipeline: crates
package_name: bevy_chat
version: "0.0.0"
source_path: packages/rust/bevy/bevy_chat
version_source: apps/kbve/astro-kbve/src/content/docs/project/bevy-chat-crate.mdx
version_target: packages/rust/bevy/bevy_chat/Cargo.toml
version_toml: packages/rust/bevy/bevy_chat/version.toml
author: h0lybyte
license: MIT
status: active
jsonld:
    faq:
        - question: What is the bevy_chat crate?
          answer: bevy_chat is a headless async IRC client that bridges chat and events for MUD and game clients. It exposes an identical public API across native tokio TCP and WASM WebSocket targets, with an optional Bevy plugin that forwards IRC traffic into ECS events.
        - question: How does bevy_chat pick a transport?
          answer: The transport is selected at compile time via cfg(target_arch). Native builds use tokio TCP to an IRC server on port 6667, while WASM builds use web_sys::WebSocket against an IRC-over-WebSocket gateway such as wss://chat.kbve.com.
        - question: What optional features does bevy_chat offer?
          answer: The plugin feature adds a Bevy ChatPlugin that bridges IRC traffic into ECS events, and the ffi feature exposes a C-FFI surface for JNI and csbindgen consumers on native targets. The default build is the headless client with neither pulled in.
---

## Overview

`bevy_chat` is a headless async IRC client and event bridge for MUD and game
clients. It provides a unified `ChatClient` API across native
([tokio](https://tokio.rs/) TCP) and WASM (`web_sys::WebSocket`) targets, with an
optional [Bevy](https://bevyengine.org/) plugin that bridges IRC traffic into ECS
events. It is used by the KBVE Discord bot, the Lightyear server, and the
in-browser isometric game.

## Key facts

| | |
|---|---|
| **Package** | `bevy_chat` |
| **Language** | Rust (edition 2024) |
| **License** | MIT |
| **Source** | [`packages/rust/bevy/bevy_chat`](https://github.com/KBVE/kbve/tree/main/packages/rust/bevy/bevy_chat) |
| **Categories** | game-development, network-programming |

## Features

- **Dual transport, one API** — native builds use tokio TCP to IRC on port 6667;
  WASM builds use `web_sys::WebSocket` against an IRC-over-WebSocket gateway
  (`wss://chat.kbve.com`). Selected at compile time via `cfg(target_arch)`.
- **Identical public surface** — `ChatClient`, `connect`, `send`, and
  `subscribe` / `drain_incoming` behave the same on both targets.
- **Bevy plugin** *(optional `plugin` feature)* — `ChatPlugin` bridges incoming
  IRC traffic into ECS events for the isometric game.
- **FFI surface** *(optional `ffi` feature, native only)* — a C-FFI layer for
  JNI and `csbindgen` consumers.

## Usage

```rust
use bevy_chat::{ChatClient, IrcConfig};

let cfg = IrcConfig::default();
let client = ChatClient::connect(cfg).await?;
```

## FAQ

**What is the bevy_chat crate?**
bevy_chat is a headless async IRC client that bridges chat and events for MUD and
game clients. It exposes an identical public API across native tokio TCP and WASM
WebSocket targets, with an optional Bevy plugin that forwards IRC traffic into ECS
events.

**How does bevy_chat pick a transport?**
The transport is selected at compile time via `cfg(target_arch)`. Native builds
use tokio TCP to an IRC server on port 6667, while WASM builds use
`web_sys::WebSocket` against an IRC-over-WebSocket gateway such as
`wss://chat.kbve.com`.

**What optional features does bevy_chat offer?**
The `plugin` feature adds a Bevy `ChatPlugin` that bridges IRC traffic into ECS
events, and the `ffi` feature exposes a C-FFI surface for JNI and `csbindgen`
consumers on native targets. The default build is the headless client with
neither pulled in.

## Related

- [Bevy Cam Crate](/docs/project/bevy-cam-crate) — isometric camera crate for the same engine.
- [uevy skills](/docs/project/bevy-skills-crate) — skill progression crate that shares the Bevy plugin pattern.
