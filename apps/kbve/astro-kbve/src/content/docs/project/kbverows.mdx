---
title: KBVEROWS — Rust Open World Server Backend Plugin for UE5
description: KBVEROWS is a unified Unreal Engine 5 backend plugin for the Rust Open World Server — auth, instance management, and character persistence over ROWS REST.
sem: 1
sidebar:
    label: KBVEROWS
    order: 86
tags:
    - unreal
    - server
key: ue_kbverows
pipeline: unreal
plugin_name: KBVEROWS
plugin_path: packages/unreal/KBVEROWS
version: "1.0.0"
source_path: packages/unreal/KBVEROWS
version_toml: packages/unreal/KBVEROWS/version.toml
author: h0lybyte
license: KBVE
status: active
min_engine_version: "5.4"
supported_platforms:
    - Win64
    - Linux
    - Mac
jsonld:
    faq:
        - question: What is KBVEROWS?
          answer: KBVEROWS is a unified Unreal Engine 5 backend integration plugin for the Rust Open World Server (ROWS). It provides auth, instance management, character persistence, and global data subsystems that talk to the ROWS REST API.
        - question: What subsystems does KBVEROWS provide?
          answer: The ROWS module ships subsystems including ROWSSubsystem, ROWSAuthSubsystem, ROWSCharacterSubsystem, and ROWSInstanceSubsystem, plus login UI widgets and a controller. Transport is pluggable via ROWSTransport with HTTP and gRPC implementations.
        - question: Which platforms and engine versions does KBVEROWS support?
          answer: KBVEROWS targets Unreal Engine 5.4 and later on Win64, Linux, and Mac. It is enabled by default and is currently a beta plugin.
---

## Overview

`KBVEROWS` is a unified Unreal Engine 5 backend integration plugin for the **Rust
Open World Server (ROWS)**. It provides auth, instance management, character
persistence, and global data subsystems that communicate with the ROWS REST API,
so an Unreal client can sign in, manage character data, and join server
instances through a single plugin.

## Key facts

| | |
|---|---|
| **Plugin** | `KBVEROWS` (module `ROWS`) |
| **Version** | 1.0 (beta) |
| **Engine** | Unreal Engine 5.4+ |
| **License** | KBVE |
| **Source** | [`packages/unreal/KBVEROWS`](https://github.com/KBVE/kbve/tree/main/packages/unreal/KBVEROWS) |
| **Platforms** | Win64, Linux, Mac |

## Features

- **Auth** — `ROWSAuthSubsystem` with login widgets (`ROWSLoginWidget`,
  `ROWSLoadingWidget`) and a `ROWSLoginController`.
- **Character persistence** — `ROWSCharacterSubsystem` for character data.
- **Instance management** — `ROWSInstanceSubsystem` to allocate and join server
  instances.
- **Pluggable transport** — `ROWSTransport` with `ROWSHttpTransport` and
  `ROWSGrpcTransport` backends.

## FAQ

**What is KBVEROWS?**
`KBVEROWS` is a unified Unreal Engine 5 backend integration plugin for the Rust
Open World Server (ROWS). It provides auth, instance management, character
persistence, and global data subsystems that talk to the ROWS REST API.

**What subsystems does KBVEROWS provide?**
The `ROWS` module ships subsystems including `ROWSSubsystem`, `ROWSAuthSubsystem`,
`ROWSCharacterSubsystem`, and `ROWSInstanceSubsystem`, plus login UI widgets and
a controller. Transport is pluggable via `ROWSTransport` with HTTP and gRPC
implementations.

**Which platforms and engine versions does KBVEROWS support?**
KBVEROWS targets Unreal Engine 5.4 and later on Win64, Linux, and Mac. It is
enabled by default and is currently a beta plugin.

## Related

- [Agones](/docs/project/agones) — server lifecycle SDK used alongside ROWS-backed dedicated servers.
- [KBVEYYJson](/docs/project/kbveyyjson) — fast JSON plugin used across the KBVE Unreal stack.
