---
title: Uniti
description: |
    Uniti — C FFI bridge from KBVE Bevy game logic crates to Unity via csbindgen.
sidebar:
    label: Uniti
tags:
    - crates
    - rust
    - bevy
    - ffi
    - unity
key: uniti_crate
pipeline: crates
package_name: uniti
version: "0.1.0"
source_path: packages/rust/bevy/uniti
version_source: packages/rust/bevy/uniti/Cargo.toml
version_target: packages/rust/bevy/uniti/Cargo.toml
version_toml: packages/rust/bevy/uniti/version.toml
author: h0lybyte
license: MIT
status: active
---

## Overview

`uniti` is the C FFI bridge that exposes the KBVE Bevy game-logic crates to the Unity runtime used by [RareIcon](/project/rareicon/). It compiles as a `cdylib` and is loaded by Unity as a native plugin.

Bindings on the C# side are generated by [`csbindgen`](https://github.com/Cysharp/csbindgen) at build time and committed to the Unity project as `Uniti.g.cs`.

## Bundled Crates

| Crate | Purpose |
| --- | --- |
| `bevy_pathfinding` | Grid flow-field navigation + chokepoint detection |
| `bevy_inventory` | Item / slot persistence |
| `bevy_battle` | Combat resolution |
| `bevy_skills` | Skill / proc tables |

Plus two runtime helpers compiled into the dylib so Unity does not need them on the host:

- `rusqlite` with the `bundled` feature — save-archive persistence, no system `libsqlite3` dep.
- `zstd` with default features disabled — pure-Rust compression for save blobs.

## Build Targets

Each Nx target builds the release `cdylib` and copies it into the Unity `Plugins/` tree for the matching platform.

```sh
npx nx run uniti:build:macos
npx nx run uniti:build:windows
npx nx run uniti:build:linux
npx nx run uniti:build:all
```

On macOS the target also runs `xattr -cr` and an adhoc `codesign --force --deep --sign -` so the dylib loads through Gatekeeper without a quarantine prompt.

## Generated Bindings

`build.rs` invokes `csbindgen` and writes the C# wrapper to:

```
apps/rareicon/unity-rareicon/Assets/_RareIcon/Scripts/Native/Uniti.g.cs
```

Hand-written wrapper classes live alongside it under `Native/Wrappers/` (for example `NativeWorld`).

## CI

Tracked in the dispatch manifest under the `crates` pipeline. The standard test / lint nx targets are wired:

```sh
npx nx run uniti:test
npx nx run uniti:lint
```

## License

MIT
