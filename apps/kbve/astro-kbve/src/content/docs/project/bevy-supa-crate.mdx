---
title: bevy_supa — Agnostic Supabase PostgREST Client Crate
description: bevy_supa is a transport-agnostic Supabase (PostgREST) client crate with native reqwest+rustls HTTP, a WASM fetch stub, and optional Bevy ECS integration.
sem: 1
sidebar:
    label: bevy_supa
tags:
    - crates
    - rust
    - bevy
    - supabase
    - http
key: bevy_supa_crate
pipeline: crates
package_name: bevy_supa
version: "0.0.0"
source_path: packages/rust/bevy/bevy_supa
version_source: apps/kbve/astro-kbve/src/content/docs/project/bevy-supa-crate.mdx
version_target: packages/rust/bevy/bevy_supa/Cargo.toml
version_toml: packages/rust/bevy/bevy_supa/version.toml
author: h0lybyte
license: MIT
status: active
jsonld:
    faq:
        - question: What is the bevy_supa crate?
          answer: bevy_supa is a transport-agnostic Supabase / PostgREST client. The default feature set ships the native HTTP transport (reqwest + rustls), Bevy integration is opt-in, and a WASM browser fetch transport is stubbed for later.
        - question: Can bevy_supa be used without Bevy?
          answer: Yes. The same SupaClient is used from JNI MC plugins with no Bevy and from Bevy games as a Resource when the bevy feature is enabled, so lean consumers like JNI cdylibs and CLIs stay slim.
        - question: How does bevy_supa target non-default PostgREST schemas?
          answer: SupaClient::rpc_schema sets the Content-Profile and Accept-Profile headers so PostgREST routes RPC calls to non-default schemas such as mc or tracker, and with_jwt swaps just the Authorization header so service-role and per-user JWTs coexist on one client.
---

## Overview

`bevy_supa` is a transport-agnostic Supabase / PostgREST client. The default feature set ships the `native` HTTP transport (`reqwest` + rustls). Bevy integration is opt-in so lean consumers (JNI cdylibs, CLIs) stay slim. It originally lived inside the `kbve` crate as `entity::client::supabase` and was extracted so JNI plugins, CLIs, and Bevy games can share one client without dragging diesel, axum, or tower along.

## Key facts

| | |
|---|---|
| **Package** | `bevy_supa` |
| **Language** | Rust (edition 2024) |
| **License** | MIT |
| **Source** | [`packages/rust/bevy/bevy_supa`](https://github.com/KBVE/kbve/tree/main/packages/rust/bevy/bevy_supa) |
| **Categories** | game-development, web-programming::http-client |
| **Default transport** | `native` (`reqwest` + rustls) |

## Features

| Feature | Description |
| --- | --- |
| `native` (default) | `reqwest` + rustls native HTTP transport |
| `wasm` | Browser `fetch` transport (stub — not yet implemented) |
| `bevy` | `BevySupaPlugin` + `SupaClient` Bevy `Resource` |

- **Lean by default** — no diesel, no tower, no Bevy in the default feature set; disable default features when only the type surface is needed (build scripts, codegen).
- **One client, two callers** — the same `SupaClient` serves JNI MC plugins (no Bevy) and Bevy games (`Resource`) with no newtype wrapper.
- **Schema-aware RPC** — `rpc_schema` sets `Content-Profile` / `Accept-Profile` headers to reach non-default PostgREST schemas.
- **JWT layering** — `with_jwt` swaps just the `Authorization` header so service-role and per-user JWTs coexist.

## Usage

```rust
use bevy_supa::SupaClient;

let client = SupaClient::from_env()
    .ok_or("SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY missing")?;

let resp = client
    .rpc_schema(
        "service_verify_link",
        serde_json::json!({ "p_mc_uuid": "0123", "p_code": 123456 }),
        "mc",
    )
    .await?;
```

## FAQ

**What is the bevy_supa crate?**
`bevy_supa` is a transport-agnostic Supabase / PostgREST client. The default feature set ships the native HTTP transport (`reqwest` + rustls), Bevy integration is opt-in, and a WASM browser `fetch` transport is stubbed for later.

**Can bevy_supa be used without Bevy?**
Yes. The same `SupaClient` is used from JNI MC plugins with no Bevy and from Bevy games as a `Resource` when the `bevy` feature is enabled, so lean consumers like JNI cdylibs and CLIs stay slim.

**How does bevy_supa target non-default PostgREST schemas?**
`SupaClient::rpc_schema` sets the `Content-Profile` and `Accept-Profile` headers so PostgREST routes RPC calls to non-default schemas such as `mc` or `tracker`, and `with_jwt` swaps just the `Authorization` header so service-role and per-user JWTs coexist on one client.

## Related

- [Bevy Cam Crate](/docs/project/bevy-cam-crate) — camera crate from the same Bevy package group.
- [bevy_inventory](/project/bevy-inventory-crate) — sibling slot-based inventory crate.
- [Kilobase](/project/kilobase) — the custom Supabase-based Postgres image this client can target.
