---
title: "Laser — Phaser + React Three Fiber Integration for React 19"
description: Laser is a lightweight TypeScript layer that embeds Phaser games and React Three Fiber scenes in React 19 apps via hooks, components, and a shared event bus.
sem: 1
sidebar:
    label: Laser
    order: 61
tags:
    - npm
    - typescript
    - phaser
    - react
    - gamedev
key: laser
pipeline: npm
package_name: laser
version: "0.1.5"
source_path: packages/npm/laser
version_toml: packages/npm/laser/version.toml
author: h0lybyte
license: MIT
status: active
jsonld:
    faq:
        - question: What is the Laser package?
          answer: Laser is a lightweight integration layer that embeds Phaser games and React Three Fiber (R3F) scenes inside React 19 applications, providing hooks, components, and a shared event bus for building hybrid 2D/3D game UIs.
        - question: What does Laser provide for Phaser and R3F?
          answer: For Phaser it exposes a PhaserGame component with ref access, a usePhaserGame context hook, and usePhaserEvent for event subscriptions. For R3F it provides a Stage component and a useGameLoop hook for frame-synced game logic.
        - question: What peer dependencies does Laser need?
          answer: Laser needs react and react-dom (>= 18). Phaser (>= 3.80) is optional for Phaser features, and three (>= 0.160) with @react-three/fiber and @react-three/drei is optional for R3F features — install only the ones you use.
---

## Overview

`@kbve/laser` is a lightweight integration layer for **Phaser** and **React Three
Fiber (R3F)** in React 19 applications. It provides React hooks and components for
embedding Phaser games and R3F scenes with a shared event bus, making it easy to
build hybrid 2D/3D game UIs. Laser is used across KBVE's browser games as the glue
between the game canvas and the React tree.

## Key facts

| | |
|---|---|
| **Package** | `@kbve/laser` |
| **Version** | 0.1.5 |
| **Language** | TypeScript |
| **License** | MIT |
| **Source** | [`packages/npm/laser`](https://github.com/KBVE/kbve/tree/main/packages/npm/laser) |
| **Registry** | npm |

## Features

- **Phaser integration** — `<PhaserGame>` component with ref-based access, a
  `usePhaserGame` context hook, and `usePhaserEvent` for subscribing to Phaser
  events.
- **React Three Fiber integration** — `<Stage>` component and a `useGameLoop` hook
  for frame-synced game logic.
- **Shared event bus** — `LaserEventBus` for decoupled communication between the
  Phaser and R3F layers.
- **TypeScript-first** — full type definitions with exported types for configs,
  events, and geometry primitives.

## Usage

```bash
npm install @kbve/laser
```

```tsx
import {
	PhaserGame,
	usePhaserEvent,
	Stage,
	useGameLoop,
	laserEvents,
} from '@kbve/laser';
```

## FAQ

**What is the Laser package?**
Laser is a lightweight integration layer that embeds Phaser games and React Three
Fiber (R3F) scenes inside React 19 applications, providing hooks, components, and a
shared event bus for building hybrid 2D/3D game UIs.

**What does Laser provide for Phaser and R3F?**
For Phaser it exposes a `PhaserGame` component with ref access, a `usePhaserGame`
context hook, and `usePhaserEvent` for event subscriptions. For R3F it provides a
`Stage` component and a `useGameLoop` hook for frame-synced game logic.

**What peer dependencies does Laser need?**
Laser needs `react` and `react-dom` (>= 18). Phaser (>= 3.80) is optional for
Phaser features, and `three` (>= 0.160) with `@react-three/fiber` and
`@react-three/drei` is optional for R3F features — install only the ones you use.

## Related

- [Droid](/docs/project/droid) — sibling KBVE npm package managing web, shared, and service workers.
