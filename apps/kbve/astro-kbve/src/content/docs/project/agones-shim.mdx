---
title: Agones Shim — Lifecycle Sidecar for Game Servers Without an SDK
description: A general-purpose Rust sidecar giving any game server proper Agones Ready, Health, Shutdown and Counters, driven entirely by environment variables so one image serves every fleet.
sem: 1
sidebar:
    label: Agones Shim
    order: 461
tags:
    - agones
    - rust
    - gameserver
    - sidecar
    - docker
key: agones_shim
pipeline: docker
app_name: agones-shim
version: "0.0.3"
source_path: apps/agones/shim
version_toml: apps/agones/shim/version.toml
runner: ubuntu-latest
image: kbve/agones-shim
nx_project: agones-shim
target: container
deployment_yamls:
    - apps/kube/agones/tocloud9/manifests/worldserver-fleet.yaml
    - apps/kube/agones/tocloud9/manifests/worldserver-b-fleet.yaml
    - apps/kube/agones/tocloud9/manifests/gateway-fleet.yaml
    - apps/kube/agones/tocloud9/manifests/authserver-fleet.yaml
has_test: false
author: h0lybyte
license: KBVE
status: active
kube:
    stack: agones
    environment: prod
    tier: server
    owner: h0lybyte
    criticality: high
bento:
    badge: agones · sidecar · rust
    title: Lifecycle sidecar
    accent: Agones Shim
    lede: >-
        Most game servers have no Agones SDK, so every fleet grows its own shell
        shim. This is one small Rust binary that does the job properly — probes,
        Ready, Health, Shutdown and Counters — configured entirely by environment
        variables so a single image serves any fleet.
    ctas:
        - label: View source
          href: https://github.com/KBVE/kbve/tree/main/apps/agones/shim
          primary: true
          external: true
        - label: Configuration
          href: "#config"
        - label: Why it exists
          href: "#why"
    aside:
        icon: activity
        title: What a shell shim cannot do
        body: >-
            A <code>nc</code> loop can tell you a port is open. It cannot tell you
            the server registered with its service registry, cannot call
            <code>Shutdown()</code> on SIGTERM, and cannot report how many players
            are connected.
        points:
            - <strong>Deliberate drain</strong> — SIGTERM becomes Shutdown(), so the grace period is actually used.
            - <strong>Honest readiness</strong> — an HTTP probe can mean "routable", not merely "bound".
            - <strong>Occupancy</strong> — publishes a Counter, which a FleetAutoscaler can scale on.
    stats:
        - { label: Image, value: kbve/agones-shim, icon: box }
        - { label: Size, value: 17.8 MB, icon: package }
        - { label: SDK, value: agones 1.58, icon: server }
        - { label: License, value: MIT, icon: license }
    features:
        - title: Graceful shutdown
          icon: shield
          body: Traps SIGTERM and calls <code>Shutdown()</code> so Agones records a deliberate drain instead of an unhealthy pod.
        - title: Probes that mean something
          icon: activity
          body: TCP for "bound", HTTP for "answering". Multiple probes, comma separated, all must pass before Ready.
        - title: Counters
          icon: network
          body: Scrapes a Prometheus gauge and publishes it as a Counter, giving a FleetAutoscaler something real to scale on.
        - title: Degrades cleanly
          icon: book
          body: With no SDK reachable it runs probe-only, so the same image works under docker compose where there is no Agones.
    related:
        - title: ToCloud9 Gameserver
          href: /project/tocloud9-gameserver/
          copy: The clustered AzerothCore worldserver this was built for.
          icon: server
        - title: Agones
          href: /project/agones/
          copy: The game-server orchestration platform.
          icon: server
    related_heading: Related
template: splash
tableOfContents: false
editUrl: false
lastUpdated: false
prev: false
next: false
---

import BentoDoc from '@/components/hero/BentoDoc.astro';
import BentoProse from '@/components/hero/BentoProse.astro';

<BentoDoc data={frontmatter.bento} faq={frontmatter.jsonld?.faq}>

<BentoProse id="why" eyebrow="Motivation" heading="Why it exists">

Game servers rarely speak the Agones SDK. AzerothCore doesn't, and neither do most dedicated servers, so each fleet ends up with a hand-rolled shim inside its manifest — poll a socket with `nc`, post to the SDK's HTTP endpoint with `wget`. ToCloud9 had three copies of exactly that, and they shared three defects.

**Nothing ever called `Shutdown()`.** On a scale-down or rolling update the pod takes SIGTERM, the shim dies, and Agones records the server as *Unhealthy* rather than shut down — so the `terminationGracePeriodSeconds` the manifest asks for is never actually spent letting the game flush state.

**Readiness meant "the socket accepts", which is not "reachable".** A ToCloud9 worldserver binds its port and only *then* registers with `servers-registry`. In that window Agones believes the server is good while the gateway cannot route anyone to it.

**Nothing reported occupancy**, so a `FleetAutoscaler` had nothing to scale on and the fleet sat at a fixed replica count.

</BentoProse>

<BentoProse id="config" eyebrow="Configuration" heading="Environment">

Nothing is game-specific — every behaviour is an environment variable, so one image serves any fleet.

| Variable | Purpose |
|---|---|
| `AGONES_SHIM_READY_TCP` | Ports that must accept before Ready. `8085` or `host:8085`, comma separated |
| `AGONES_SHIM_READY_HTTP` | URLs that must return 2xx before Ready |
| `AGONES_SHIM_LIVE_TCP` / `_HTTP` | Liveness probes; defaults to the readiness set |
| `AGONES_SHIM_READY_TIMEOUT_SECS` | Give up waiting. `0` (default) means never |
| `AGONES_SHIM_HEALTH_INTERVAL_SECS` | Health beat and liveness interval, default `5` |
| `AGONES_SHIM_COUNTER_NAME` | Counter to publish; must already exist in the GameServer spec |
| `AGONES_SHIM_COUNTER_METRICS_URL` | Prometheus text endpoint to scrape |
| `AGONES_SHIM_COUNTER_METRIC` | Metric name to read from it |
| `AGONES_SHIM_COUNTER_INTERVAL_SECS` | Scrape interval, default `30` |
| `AGONES_SHIM_SDK_CONNECT_TIMEOUT_SECS` | Bound on the SDK connection, default `30` |

Prefer an HTTP probe wherever the server exposes one. For the ToCloud9 worldserver, libsidecar's health server starts within milliseconds of registration and well after the world socket binds — so a `200` on `:8901` means *registered and routable*, a signal the socket check simply cannot express.

</BentoProse>

<BentoProse id="notes" eyebrow="Design" heading="Decisions worth knowing">

**The SDK connection is bounded on purpose.** `Sdk::new` does not fail fast when nothing is listening — it just sits there — and every step below it, readiness included, is behind that call. Unbounded, a slow-starting sidecar means the GameServer is never marked Ready and Agones kills a pod that was healthy all along.

**A failed Counter scrape keeps the previous value.** Reporting zero because a metrics endpoint blipped would tell an autoscaler the server had emptied.

**A dead game server exits without `Shutdown()`.** If a liveness probe fails the shim stops beating health and exits non-zero, so Agones replaces the pod. Calling `Shutdown()` there would report a clean exit for a server that actually crashed.

**Counters must be declared in the GameServer spec.** The SDK can only update a Counter that already exists; `set_counter_count` on an undeclared name fails.

</BentoProse>

<BentoProse id="scope" eyebrow="Adoption" heading="What is not migrated">

Factorio and Palworld already have their own shims, and both do more than lifecycle: they trap SIGTERM and flush a save through RCON before shutting down. That is game-specific work this deliberately does not replicate.

They could adopt this for the lifecycle plumbing and keep their save logic — the shim's liveness probe would sit alongside a game-specific PreStop hook — but that is a separate migration with its own risk, and their current shims are not broken.

</BentoProse>

</BentoDoc>
