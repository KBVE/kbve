---
title: Ruby harvest (item) | OSRS Price Data
description: "Ruby harvest (item): There's a ruby harvest butterfly in here.. High alch: 10 GP, GE limit: 125."
osrs:
  id: 10020
  name: Ruby harvest (item)
  slug: ruby-harvest-item
  examine: There's a ruby harvest butterfly in here.
  members: true
  icon: Ruby harvest (item).png
  value: 18
  lowalch: 7
  highalch: 10
  limit: 125
---

import OSRSItemPanel from '@/components/osrs/OSRSItemPanel.astro';
import { Adsense } from '@/components/astropad';

<OSRSItemPanel data={frontmatter.osrs} />

## About {frontmatter.osrs.name}

**{frontmatter.osrs.name}** is a members-only item in Old School RuneScape.

> "{frontmatter.osrs.examine}"

## Item Details

- **Item ID:** {frontmatter.osrs.id}
- **Members:** {frontmatter.osrs.members ? 'Yes' : 'No'}
- **Store Value:** {frontmatter.osrs.value?.toLocaleString()} GP
- **High Alch:** {frontmatter.osrs.highalch?.toLocaleString()} GP
- **Low Alch:** {frontmatter.osrs.lowalch?.toLocaleString()} GP
- **GE Limit:** {frontmatter.osrs.limit?.toLocaleString()} per 4 hours

<Adsense />
