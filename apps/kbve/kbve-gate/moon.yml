$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'rust'
tags: ['rust', 'docker']

tasks:
    containerx:
        script: 'docker buildx build --load -f apps/kbve/kbve-gate/Dockerfile -t kbve/kbve-gate:latest .'
        options:
            runFromWorkspaceRoot: true
            cache: false
