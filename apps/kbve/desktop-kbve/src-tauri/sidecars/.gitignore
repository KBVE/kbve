# Per-crate build output
*/target/
# Staged externalBin binaries (produced by build.sh)
llm-sidecar-*
tts-sidecar-*
memory-sidecar-*
discord-sidecar-*
mistralrs-sidecar-*

# espeak-ng data staged from brew (see build.sh)
espeak-ng-data/
rmlx-*
.rmlx-install/
