---
export type { Props } from './inter';
let { load, ve, data, widget } = Astro.props;
let html = '';
import Wrapper from './lib/Wrapper.astro';

// const runVirtualEngine = async () => {
// 	const _components = await Astro.glob('./lib/*.astro');
// 	_components.filter((comp) => console.log(`${comp.file}`));
// };
---

{
	!load && (
		<Wrapper img="https://kbve.com/assets/img/curved-images/wave.jpg">
			<p>
				AstroVe works! Yippy Test {data} {ve} {load}
			</p>
		</Wrapper>
	)
}
