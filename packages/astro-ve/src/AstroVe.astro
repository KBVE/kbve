---
interface Props {
  //* Load
  load?: 'none' | 'lazy' | 'partytown' | 'helmet' | 'true';
  //* VE
  ve?: string;
  //* Data
  data?: string;
  //* Widget
  widget?: string;
}
let { data = 'hello world', ve = '1', load = 'true' } = Astro.props;

//const runVirtualEngine = () => {};
---

<p>AstroVe works! Yippy Test {data} {ve} {load}</p>
