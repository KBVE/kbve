---
export interface Props {
	title: string;
	description: string;
  products: any;
}

const { title, description, products } = Astro.props;

import React from "react";
import { LottieCardDemo } from "./lottie-card";

---
<LottieCardDemo client:only="react" />