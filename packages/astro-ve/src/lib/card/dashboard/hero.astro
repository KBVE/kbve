---
//	Dev Only Hero - DO NOT USE IN PRODUCTION!
export interface Props {
	id?: string;
	dyn?: boolean;
	img?: string;
	description?: string;
}

const { id, dyn = false, img } = Astro.props;

import { style, dynStyle } from '../../style';

let styleSheet = dyn ? dynStyle : style;

let _internal: string = 'card:dashboard:hero';
let idText: string = 'PlaceHolder';
let descriptionText: string =
	'Here are the biggest enterprise technology acquisitions of 2021 so far, in reverse chronological order.';
let _href: string = '#';
let _img: string = 'https://source.unsplash.com/random/480x360';

// Dynamic CSS Placeholders
let baseBackgroundStyle: string =
	styleSheet.getVariable('bg-kbve-menu-bg').variable;

let baseTextStyle: string = styleSheet.getVariable(
	'text-kbve-text-primary'
).variable;
let baseSecondaryTextStyle: string = styleSheet.getVariable(
	'text-kbve-text-secondary'
).variable;
let baseSVGColorStyle: string = styleSheet.getVariable(
	'text-kbve-svg-primary'
).variable;

if (img) {
	_img = img;
}
---

<Fragment>

	<section
		class={`flex-auto max-w-4xl xl:w-full md:w-6/12 p-5 ${baseBackgroundStyle} border border-cyan-200 rounded-lg shadow bg-default bg-cover`}
		style={`background-image: url('${_img}')`}>
			<div
				class="w-full flex justify-center max-h-120 px-2 py-4 rounded-md sm:px-10 md:px-12 xl:col-span-2 bg-default bg-cover bg-blend-overlay"
				style="background-color: rgba(187, 215, 233, 0.4); backdrop-filter: blur(2px);">
				<div class="flex flex-col justify-center items-center">
					<h1 class="md:text-4xl text-5xl font-extrabold bg-gradient-to-r from-cyan-200 via-blue-200 to-pink-200 bg-clip-text text-transparent">
						Meet Your Dream Dev Team!
					</h1>
					<p class="my-2">
						<span class="md:text-2xl text-lg font-medium text-white">Crafting Your Digital Dreams from Vision to Virtual. From Apps to Games to Media.</span>
					</p>
					<form novalidate="" action="" class="flex flex-col self-stretch space-y-2 pt-4">
						<div>
							<label for="name" class="text-sm sr-only">
								Your name
							</label>
							<input
								id="fullname"
								type="text"
								placeholder="Name"
								class="w-full rounded-md focus:ring focus:ri dark:border-gray-700 p-2 backdrop-blur-sm"
								style="background-color: rgba(255, 255, 255, 0.6);"
							/>
						</div>
						<div>
							<label for="lastname" class="text-sm sr-only">
								Email address
							</label>
							<input
								id="email"
								type="text"
								placeholder="Email Address"
								class="w-full rounded-md focus:ring focus:ri dark:border-gray-700 p-2 backdrop-blur-sm"
								style="background-color: rgba(255, 255, 255, 0.6);"
							/>
						</div>

						<button class="relative inline-block text-lg group pt-4">
							<span
								class="relative z-10 block px-5 py-3 overflow-hidden font-medium leading-tight text-gray-800 transition-colors duration-300 ease-out border-2 border-gray-900 rounded-lg group-hover:text-white">
								<span
									class="absolute inset-0 w-full h-full px-5 py-3 rounded-lg bg-gray-50">
								</span>
								<span
									class="absolute left-0 w-48 h-48 -ml-2 transition-all duration-300 origin-top-right -rotate-90 -translate-x-full translate-y-12 bg-gray-900 group-hover:-rotate-180 ease">
								</span>
								<span class="relative">Login!</span>
							</span>
							<span
								class="absolute bottom-0 right-0 w-full h-12 -mb-1 -mr-1 transition-all duration-200 ease-linear bg-gray-900 rounded-lg group-hover:mb-0 group-hover:mr-0"
								data-rounded="rounded-lg">
							</span>
						</button>
					</form>

					<p class="my-2 p-4 rounded-lg bg-zinc-900/[.20]">
						<span class="md:text-2xl text-lg font-bold bg-gradient-to-r from-cyan-300 via-rose-400 to-yellow-400 bg-clip-text text-transparent">Modular Mastery in Every Byte. Building Blocks for Tomorrow's Software. Transforming Ideas into Reality with KBVE.</span>
					</p>

				</div>
			</div>

			<!--
                <img src={`${_img}`} alt="" class="object-cover w-full rounded-md xl:col-span-3 dark:bg-gray-500">
             -->
			
			 <slot />
	</section>
</Fragment>
