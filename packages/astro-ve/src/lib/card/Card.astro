---
export interface Props {
	widget: string;
	data?: string;
}
const { data, widget } = Astro.props;
let mod = widget.split(':')[1];

import Demo from './demo/Demo.astro';
---
{
	mod == 'demo' && (
		<Demo data={data}>
			<slot name="raw" slot="raw" />
			<slot />
		</Demo>
	)
}
