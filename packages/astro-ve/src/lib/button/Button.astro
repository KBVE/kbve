---
export interface Props {
	widget: string;
	data?: string;
}
const { data, widget } = Astro.props;
---

{
	widget == 'button:demo' && (
		<>
			<slot name="raw" slot="raw" />
			<slot />
		</>
	)
}
