---
export interface Props {
	id?: string;
	dyn?: string;
}

const { id, dyn = false } = Astro.props;

//	Slots

//	TODO

//	Imports
import { style, dynStyle } from '../../style';

let styleSheet = dyn ? dynStyle : style;

//	Dynamic CSS Placeholders

let baseBackgroundStyle: string =
	styleSheet.getVariable('bg-kbve-menu-bg').variable;

let baseSVGStyle: string = styleSheet.getVariable(
	'bg-kbve-svg-primary'
).setHover().variable;

---

<Fragment>
	<aside
		class={`h-full w-16 flex flex-col space-y-10 items-center justify-center relative ${baseBackgroundStyle} text-white`}>
		

		<slot />

	</aside>
</Fragment>
