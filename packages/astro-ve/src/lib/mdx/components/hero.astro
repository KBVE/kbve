---
// TODO : Hero for MDX
export interface Props {
    id?: string;
    dyn?: string;
    content: any;
}
const { id, dyn = false, content } = Astro.props;

let _id = '';
---

<Fragment>

</Fragment>