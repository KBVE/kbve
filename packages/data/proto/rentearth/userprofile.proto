syntax = "proto3";

package rentearth;

message UserProfile {
    uint64 id = 1;       // id could race condition but is meant as a quick ref.
    uint64 ulid = 2;     // ULID of the user.
    string uuid = 3;     // UUID of the user.
    string username = 4; // String will be the username.
}