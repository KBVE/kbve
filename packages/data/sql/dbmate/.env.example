# dbmate connection string
#
# IMPORTANT: Always include search_path=dbmate,public so migration tracking
# lives in the dbmate schema (not public), keeping it isolated from PostgREST.
#
# Local dev (dev-docker-compose.yml, db=postgres):
DATABASE_URL="postgresql://postgres:postgres@localhost:54322/postgres?sslmode=disable&search_path=dbmate,public"
#
# Production replica (docker-compose.yml with CNPG image, db=supabase):
# DATABASE_URL="postgresql://postgres:postgres@localhost:54322/supabase?sslmode=disable&search_path=dbmate,public"
#
# Production (via kubectl port-forward, db=supabase):
# DATABASE_URL="postgresql://postgres:postgres@localhost:54322/supabase?sslmode=disable&search_path=dbmate,public"
