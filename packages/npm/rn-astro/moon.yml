$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'library'
language: 'typescript'
tags: ['eslint']

tasks:
    typecheck:
        script: 'tsc --noEmit -p packages/npm/rn-astro/tsconfig.lib.json'
        options:
            runFromWorkspaceRoot: true
