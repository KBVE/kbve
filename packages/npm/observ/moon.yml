$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'library'
language: 'typescript'
tags: ['eslint']

tasks:
    typecheck:
        script: 'tsc --noEmit -p packages/npm/observ/tsconfig.lib.json'
        options:
            runFromWorkspaceRoot: true
