// @generated
// This file is @generated by prost-build.
/// What a token is.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct TokenProfile {
    /// The chain it lives on, as a lowercase slug: "ethereum", "solana", "base".
    /// A token bridged to several chains is several assets that happen to share a
    /// name, and treating them as one is how a supply figure ends up counted
    /// twice.
    #[prost(string, tag="1")]
    pub chain: ::prost::alloc::string::String,
    /// The chain's own numeric id, where it has one: EIP-155 for an EVM chain,
    /// 1 for Ethereum mainnet, 8453 for Base. The slug above is what humans and
    /// data sources call it, and they disagree -- "eth", "ethereum", "mainnet",
    /// "erc20" all appear for the same chain. This is what actually keys it, and
    /// a contract address without it is not a unique address: the same bytes are
    /// a different contract on every EVM chain, and the same deployer commonly
    /// deploys to all of them.
    #[prost(int64, optional, tag="16")]
    pub chain_id: ::core::option::Option<i64>,
    #[prost(enumeration="TokenStandard", tag="2")]
    pub standard: i32,
    /// Where it lives on that chain. Absent for a native coin, which has no
    /// contract.
    #[prost(string, optional, tag="3")]
    pub contract_address: ::core::option::Option<::prost::alloc::string::String>,
    /// How many decimal places the on-chain integer carries. Eighteen for most
    /// ERC-20s, six for USDC, eight for BTC.
    ///
    /// This is the token's own unit, not a display preference: reading a raw
    /// balance without it is wrong by a factor of ten to the eighteenth, and it
    /// is the most common way a crypto integration reports a number that is
    /// absurd rather than merely inaccurate.
    #[prost(int32, optional, tag="4")]
    pub decimals: ::core::option::Option<i32>,
    /// Units that exist and are not locked, burned, or unissued. The only supply
    /// figure that belongs in a market capitalisation.
    #[prost(message, optional, tag="5")]
    pub circulating_supply: ::core::option::Option<super::super::common::v1::Decimal>,
    /// Units that exist, including locked and vesting.
    #[prost(message, optional, tag="6")]
    pub total_supply: ::core::option::Option<super::super::common::v1::Decimal>,
    /// The ceiling, where the protocol has one. Absent means uncapped, which is
    /// not the same as unknown -- and a consumer that cannot tell those apart
    /// will render an inflation schedule that does not exist.
    #[prost(message, optional, tag="7")]
    pub max_supply: ::core::option::Option<super::super::common::v1::Decimal>,
    /// Price times circulating supply. Recomputed rather than trusted: a cap
    /// built on total supply instead of circulating overstates a token with a
    /// large locked allocation by a multiple, and the two figures are reported
    /// interchangeably by sources that should know better.
    #[prost(message, optional, tag="8")]
    pub market_cap: ::core::option::Option<super::super::common::v1::Decimal>,
    /// Price times max supply, where one exists.
    #[prost(message, optional, tag="9")]
    pub fully_diluted_valuation: ::core::option::Option<super::super::common::v1::Decimal>,
    /// When the supply figures above were read. Supply is not static and is not
    /// quoted: it changes on unlocks, burns and emissions that happen on their
    /// own schedule, and a source that returns it alongside a live price implies
    /// the two are equally fresh when the supply may be weeks old. A market
    /// capitalisation is only as current as its stalest input, and it is never
    /// the price.
    #[prost(message, optional, tag="17")]
    pub supply_as_of: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(enumeration="PegKind", optional, tag="10")]
    pub peg: ::core::option::Option<i32>,
    /// What it is pegged to, where it is pegged: "USD", "XAU".
    #[prost(string, optional, tag="11")]
    pub peg_currency: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(message, optional, tag="12")]
    pub genesis_at: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(string, optional, tag="13")]
    pub whitepaper_url: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(string, optional, tag="14")]
    pub website_url: ::core::option::Option<::prost::alloc::string::String>,
    /// The canonical explorer entry for the contract, so a holding can be
    /// verified against the chain rather than against this record.
    #[prost(string, optional, tag="15")]
    pub explorer_url: ::core::option::Option<::prost::alloc::string::String>,
}
// What a token is, beyond the fact that it has a price.
//
// The equity analogue is EquityProfile, and the difference that matters is
// supply. A company's share count changes on a schedule and is audited; a
// token's does not have to be either. Most of what goes wrong with crypto data
// goes wrong here.

/// How a token exists.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum TokenStandard {
    Unspecified = 0,
    /// The chain's own unit: ETH on Ethereum, SOL on Solana.
    Native = 1,
    Erc20 = 2,
    Erc721 = 3,
    Erc1155 = 4,
    Spl = 5,
    Bep20 = 6,
}
impl TokenStandard {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "TOKEN_STANDARD_UNSPECIFIED",
            Self::Native => "TOKEN_STANDARD_NATIVE",
            Self::Erc20 => "TOKEN_STANDARD_ERC20",
            Self::Erc721 => "TOKEN_STANDARD_ERC721",
            Self::Erc1155 => "TOKEN_STANDARD_ERC1155",
            Self::Spl => "TOKEN_STANDARD_SPL",
            Self::Bep20 => "TOKEN_STANDARD_BEP20",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "TOKEN_STANDARD_UNSPECIFIED" => Some(Self::Unspecified),
            "TOKEN_STANDARD_NATIVE" => Some(Self::Native),
            "TOKEN_STANDARD_ERC20" => Some(Self::Erc20),
            "TOKEN_STANDARD_ERC721" => Some(Self::Erc721),
            "TOKEN_STANDARD_ERC1155" => Some(Self::Erc1155),
            "TOKEN_STANDARD_SPL" => Some(Self::Spl),
            "TOKEN_STANDARD_BEP20" => Some(Self::Bep20),
            _ => None,
        }
    }
}
/// What, if anything, a stablecoin is backed by.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum PegKind {
    Unspecified = 0,
    /// Not pegged.
    None = 1,
    /// Reserves held off-chain: cash, treasuries.
    FiatBacked = 2,
    /// Over-collateralised with on-chain assets.
    CryptoBacked = 3,
    /// Held by a mint-and-burn mechanism with no reserve behind it.
    Algorithmic = 4,
}
impl PegKind {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "PEG_KIND_UNSPECIFIED",
            Self::None => "PEG_KIND_NONE",
            Self::FiatBacked => "PEG_KIND_FIAT_BACKED",
            Self::CryptoBacked => "PEG_KIND_CRYPTO_BACKED",
            Self::Algorithmic => "PEG_KIND_ALGORITHMIC",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "PEG_KIND_UNSPECIFIED" => Some(Self::Unspecified),
            "PEG_KIND_NONE" => Some(Self::None),
            "PEG_KIND_FIAT_BACKED" => Some(Self::FiatBacked),
            "PEG_KIND_CRYPTO_BACKED" => Some(Self::CryptoBacked),
            "PEG_KIND_ALGORITHMIC" => Some(Self::Algorithmic),
            _ => None,
        }
    }
}
/// What a fund is.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct FundProfile {
    #[prost(enumeration="FundKind", tag="1")]
    pub kind: i32,
    /// Annual cost, in basis points: 3 is a broad-market index fund, 95 is an
    /// active one. Deducted from the fund's assets rather than billed, so it
    /// never appears in a price series and a return computed from prices alone
    /// has already had it taken out without saying so.
    #[prost(int32, optional, tag="2")]
    pub expense_ratio_bps: ::core::option::Option<i32>,
    /// Total assets under management, in the currency below. The liquidity
    /// figure that matters for a fund: a large fund with thin volume can still
    /// be traded, because a market maker can create shares against the basket.
    #[prost(message, optional, tag="3")]
    pub aum: ::core::option::Option<super::super::common::v1::Decimal>,
    #[prost(string, optional, tag="4")]
    pub currency: ::core::option::Option<::prost::alloc::string::String>,
    /// What it is trying to match, as the provider words it, e.g.
    /// "S&P 500", "MSCI World". Absent on an active fund.
    #[prost(string, optional, tag="5")]
    pub benchmark_index: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(enumeration="ReplicationMethod", tag="6")]
    pub replication: i32,
    /// Leverage, in basis points: 20000 is a two-times fund, 10000 is unlevered.
    /// Levered and inverse funds reset daily, so their return over any period
    /// longer than a day is not their multiple of the index return -- it is path
    /// dependent, and the gap grows with volatility. Recording the factor is what
    /// lets a consumer refuse to annualise them.
    #[prost(int32, optional, tag="7")]
    pub leverage_bps: ::core::option::Option<i32>,
    /// Whether it returns the inverse of the benchmark.
    #[prost(bool, optional, tag="8")]
    pub inverse: ::core::option::Option<bool>,
    /// Distributions per year: 12 monthly, 4 quarterly, 0 for an accumulating
    /// fund that reinvests instead. Zero and unknown are different answers here,
    /// which is why this is optional rather than defaulted.
    #[prost(int32, optional, tag="9")]
    pub distribution_frequency: ::core::option::Option<i32>,
    /// Whether income is reinvested rather than paid out. An accumulating fund's
    /// price series is already a total-return series; a distributing fund's is
    /// not, and comparing the two directly understates the second.
    #[prost(bool, optional, tag="10")]
    pub accumulating: ::core::option::Option<bool>,
    /// Where the fund is domiciled, as ISO 3166-1 alpha-2. Decides withholding
    /// and eligibility -- a US-domiciled ETF is not purchasable by most European
    /// retail investors regardless of where it lists.
    #[prost(string, optional, tag="11")]
    pub domicile: ::core::option::Option<::prost::alloc::string::String>,
    /// Whether it is UCITS-compliant, which is the form that answers the above.
    #[prost(bool, optional, tag="12")]
    pub ucits: ::core::option::Option<bool>,
    #[prost(int32, optional, tag="13")]
    pub holdings_count: ::core::option::Option<i32>,
    #[prost(message, optional, tag="14")]
    pub inception_at: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(string, optional, tag="15")]
    pub issuer_name: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(string, optional, tag="16")]
    pub prospectus_url: ::core::option::Option<::prost::alloc::string::String>,
}
// What a fund is, beyond the fact that it has a price.
//
// AssetClass carries FUND and EquityProfile carries `is_etf`, and between them
// they say a thing is a fund without saying anything about it. That is a
// larger hole than it looks: a fund's price is not what it is worth, its cost
// is not in the price at all, and two funds tracking the same index can differ
// by more than the index moved. None of that is visible from a quote.

/// How a fund is structured.
///
/// The structure decides whether the price can leave the value behind. An ETF
/// creates and redeems shares continuously, so it tracks; a closed-end fund has
/// a fixed share count and trades at whatever someone will pay, which is
/// routinely a fifth away from what it holds.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum FundKind {
    Unspecified = 0,
    Etf = 1,
    /// Priced once a day at net asset value, with no intraday market.
    MutualFund = 2,
    /// Fixed share count. Premium and discount to NAV are the whole story.
    ClosedEnd = 3,
    /// An unsecured debt obligation that pays an index return. Carries issuer
    /// credit risk that an ETF holding the same index does not, which is the one
    /// fact a holder is most likely not to know.
    Etn = 4,
    /// A grantor trust holding a physical asset, e.g. a metal.
    Trust = 5,
}
impl FundKind {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "FUND_KIND_UNSPECIFIED",
            Self::Etf => "FUND_KIND_ETF",
            Self::MutualFund => "FUND_KIND_MUTUAL_FUND",
            Self::ClosedEnd => "FUND_KIND_CLOSED_END",
            Self::Etn => "FUND_KIND_ETN",
            Self::Trust => "FUND_KIND_TRUST",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "FUND_KIND_UNSPECIFIED" => Some(Self::Unspecified),
            "FUND_KIND_ETF" => Some(Self::Etf),
            "FUND_KIND_MUTUAL_FUND" => Some(Self::MutualFund),
            "FUND_KIND_CLOSED_END" => Some(Self::ClosedEnd),
            "FUND_KIND_ETN" => Some(Self::Etn),
            "FUND_KIND_TRUST" => Some(Self::Trust),
            _ => None,
        }
    }
}
/// How a fund gets the return it advertises.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum ReplicationMethod {
    Unspecified = 0,
    /// Holds every constituent at index weight.
    Full = 1,
    /// Holds a subset chosen to behave like the index. Introduces tracking error
    /// that full replication does not have.
    Sampled = 2,
    /// Holds a swap against a counterparty rather than the assets. The return is
    /// contractual, so it comes with counterparty risk instead of tracking error.
    Synthetic = 3,
    /// No index. A manager picks the holdings.
    Active = 4,
}
impl ReplicationMethod {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "REPLICATION_METHOD_UNSPECIFIED",
            Self::Full => "REPLICATION_METHOD_FULL",
            Self::Sampled => "REPLICATION_METHOD_SAMPLED",
            Self::Synthetic => "REPLICATION_METHOD_SYNTHETIC",
            Self::Active => "REPLICATION_METHOD_ACTIVE",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "REPLICATION_METHOD_UNSPECIFIED" => Some(Self::Unspecified),
            "REPLICATION_METHOD_FULL" => Some(Self::Full),
            "REPLICATION_METHOD_SAMPLED" => Some(Self::Sampled),
            "REPLICATION_METHOD_SYNTHETIC" => Some(Self::Synthetic),
            "REPLICATION_METHOD_ACTIVE" => Some(Self::Active),
            _ => None,
        }
    }
}
/// How an asset is named where it trades.
///
/// A symbol alone does not identify anything: the same ticker is different
/// companies on different exchanges, and a token symbol is not unique at all.
/// The venue is part of the name.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct AssetRef {
    #[prost(enumeration="AssetClass", tag="1")]
    pub asset_class: i32,
    /// Where it trades, e.g. "osrs-ge", "nasdaq", "coinbase".
    #[prost(string, tag="2")]
    pub venue: ::prost::alloc::string::String,
    /// How the venue names it, e.g. "Abyssal whip", "AAPL", "BTC-USD".
    #[prost(string, tag="3")]
    pub symbol: ::prost::alloc::string::String,
}
/// What a venue permits.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct TradingRules {
    /// How many units may be bought within the window below. The Grand Exchange
    /// calls this a buy limit; an exchange would call it a position limit.
    #[prost(int64, optional, tag="1")]
    pub buy_limit: ::core::option::Option<i64>,
    #[prost(int64, optional, tag="2")]
    pub buy_limit_window_seconds: ::core::option::Option<i64>,
    /// The smallest order the venue accepts.
    #[prost(message, optional, tag="3")]
    pub min_order_size: ::core::option::Option<super::super::common::v1::Decimal>,
    /// The smallest price increment.
    #[prost(message, optional, tag="4")]
    pub tick_size: ::core::option::Option<super::super::common::v1::Decimal>,
    /// Orders must be a multiple of this many units.
    #[prost(int64, optional, tag="5")]
    pub lot_size: ::core::option::Option<i64>,
    #[prost(bool, optional, tag="6")]
    pub tradeable: ::core::option::Option<bool>,
    /// Whether units of it are interchangeable and can be held as one balance.
    #[prost(bool, optional, tag="7")]
    pub fungible: ::core::option::Option<bool>,
    /// Whether the venue's fee schedule skips this asset. Venues keep explicit
    /// exemption lists - the Grand Exchange does not tax tools or bonds - and an
    /// exemption is a property of the asset rather than of the schedule, so it
    /// cannot live on the venue with the rate.
    #[prost(bool, optional, tag="8")]
    pub fee_exempt: ::core::option::Option<bool>,
}
/// One name an asset goes by, and the scheme that issued it.
///
/// The scheme is a lowercase slug: "isin", "cusip", "figi", "sedol", "cik",
/// "mic", "contract-address", "osrs-item-id". Keeping it open avoids an enum
/// that has to change every time a data source is added.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct AssetIdentifier {
    #[prost(string, tag="1")]
    pub scheme: ::prost::alloc::string::String,
    #[prost(string, tag="2")]
    pub value: ::prost::alloc::string::String,
}
/// What a company or fund is, beyond the fact that it trades.
///
/// Present only on equities and funds. Absent on a token or a game item, which
/// have no sector and no share count.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct EquityProfile {
    /// Where the issuer is domiciled, as an ISO 3166-1 alpha-2 code.
    #[prost(string, optional, tag="1")]
    pub country: ::core::option::Option<::prost::alloc::string::String>,
    /// Classification, as the source system words it.
    #[prost(string, optional, tag="2")]
    pub sector: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(string, optional, tag="3")]
    pub industry: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(int64, optional, tag="4")]
    pub shares_outstanding: ::core::option::Option<i64>,
    /// Recomputed rather than stored as truth: it is a price times a share count,
    /// and both move.
    #[prost(message, optional, tag="5")]
    pub market_cap: ::core::option::Option<super::super::common::v1::Decimal>,
    /// Superseded by Asset.fund, which says what kind of fund and at what cost.
    /// Kept because a source that only knows the boolean is still a source.
    #[prost(bool, optional, tag="6")]
    pub is_etf: ::core::option::Option<bool>,
    #[prost(string, optional, tag="7")]
    pub issuer_name: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(message, optional, tag="8")]
    pub listed_at: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(enumeration="SecurityType", tag="9")]
    pub security_type: i32,
    /// Which class of share this line is: "A", "B", "C". Two classes of the same
    /// company are two assets with two prices and one set of financials, and a
    /// portfolio that cannot see they are the same issuer reports diversification
    /// it does not have. Grouping is by the issuer identifier -- scheme "cik" or
    /// "lei" in Asset.identifiers -- because the class letter alone is not a key.
    #[prost(string, optional, tag="10")]
    pub share_class: ::core::option::Option<::prost::alloc::string::String>,
    /// Shares actually available to trade, which is outstanding minus what
    /// insiders, governments and cross-holdings sit on. Index weights and any
    /// honest liquidity figure use this one; market capitalisation uses the other
    /// one. For a founder-controlled company the two differ by a multiple, so
    /// silently substituting either for the other is a large error in whichever
    /// direction it is made.
    #[prost(int64, optional, tag="11")]
    pub free_float_shares: ::core::option::Option<i64>,
    /// What the company files its financials in, which is not what its listing
    /// quotes in. An ADR quotes USD and reports in EUR or JPY, and every ratio
    /// built by dividing one by the other is wrong by the exchange rate.
    #[prost(string, optional, tag="12")]
    pub reporting_currency: ::core::option::Option<::prost::alloc::string::String>,
    /// The calendar month the fiscal year ends in, 1 to 12. Without it there is
    /// no way to place a reported quarter on a calendar: a fiscal Q1 ending in
    /// December and one ending in March are a quarter apart, and comparing two
    /// companies quarter-by-quarter across that gap compares different weather.
    #[prost(int32, optional, tag="13")]
    pub fiscal_year_end_month: ::core::option::Option<i32>,
    /// The conversion between a depositary receipt and the underlying shares,
    /// as a ratio rather than a decimal: one ADR representing half an ordinary
    /// share is depositary 1, ordinary 1, then... it is not, and that is the
    /// point -- ratios here run both ways and are not always integers on the side
    /// you expect. Present only on an ADR or GDR. Per-share figures -- EPS,
    /// dividend per share, book value -- must be scaled by it before they can be
    /// compared to the receipt's own price.
    #[prost(int64, optional, tag="14")]
    pub adr_ordinary_shares: ::core::option::Option<i64>,
    #[prost(int64, optional, tag="15")]
    pub adr_depositary_shares: ::core::option::Option<i64>,
    /// Whether this listing is the company's primary one. The same company lists
    /// on several venues, and consolidating volume across all of them without
    /// knowing which is primary counts the same trading more than once.
    #[prost(bool, optional, tag="16")]
    pub primary_listing: ::core::option::Option<bool>,
}
/// A tradeable thing.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Asset {
    #[prost(message, optional, tag="1")]
    pub id: ::core::option::Option<super::super::r#type::v1::Ulid>,
    #[prost(message, optional, tag="2")]
    pub r#ref: ::core::option::Option<AssetRef>,
    #[prost(string, tag="3")]
    pub name: ::prost::alloc::string::String,
    #[prost(string, optional, tag="4")]
    pub description: ::core::option::Option<::prost::alloc::string::String>,
    /// What prices for it are quoted in: a currency code, or the name of a game
    /// currency such as "gp".
    #[prost(string, tag="5")]
    pub quote_currency: ::prost::alloc::string::String,
    /// Every identifier the asset is known by. A security carries several at
    /// once - an ISIN, a CUSIP, a FIGI, an exchange ticker - and which one a data
    /// source speaks is not a property of the asset. A single native_id field
    /// would have forced a choice that does not exist.
    #[prost(message, repeated, tag="6")]
    pub identifiers: ::prost::alloc::vec::Vec<AssetIdentifier>,
    #[prost(message, optional, tag="7")]
    pub rules: ::core::option::Option<TradingRules>,
    /// What the issuing system says it is worth, where that is a fixed number
    /// rather than a market price. A game item's shop value, a bond's face value.
    #[prost(message, optional, tag="8")]
    pub base_value: ::core::option::Option<super::super::common::v1::Decimal>,
    #[prost(message, optional, tag="9")]
    pub equity: ::core::option::Option<EquityProfile>,
    #[prost(string, optional, tag="10")]
    pub icon_url: ::core::option::Option<::prost::alloc::string::String>,
    #[prost(string, repeated, tag="11")]
    pub tags: ::prost::alloc::vec::Vec<::prost::alloc::string::String>,
    #[prost(message, optional, tag="12")]
    pub created_at: ::core::option::Option<::prost_types::Timestamp>,
    #[prost(message, optional, tag="13")]
    pub updated_at: ::core::option::Option<::prost_types::Timestamp>,
    /// When the asset stopped trading: an item removed from a game, a company
    /// delisted, a token abandoned.
    #[prost(message, optional, tag="14")]
    pub retired_at: ::core::option::Option<::prost_types::Timestamp>,
    /// Present on a token, absent on everything else, and the counterpart to
    /// equity above.
    #[prost(message, optional, tag="15")]
    pub crypto: ::core::option::Option<TokenProfile>,
    /// What this row is a market in, where the row is a pair rather than a thing.
    ///
    /// A crypto venue lists BTC-USD and BTC-EUR as separate instruments with
    /// separate prices, and they are the same asset priced two ways. Without this
    /// nothing can tell that, so a portfolio holding both double-counts the
    /// position and a chart switching between them reads as a move.
    #[prost(message, optional, tag="16")]
    pub base_asset: ::core::option::Option<AssetRef>,
    /// Present on a fund, and the counterpart to equity and crypto above. A fund
    /// is not a company: it has no earnings and its cost is deducted from its
    /// assets rather than charged, so EquityProfile describes it wrongly rather
    /// than incompletely.
    #[prost(message, optional, tag="17")]
    pub fund: ::core::option::Option<FundProfile>,
    /// What this asset is a claim on, where it is a wrapper rather than a thing.
    ///
    /// WETH is ETH, USDC on Base is USDC on Ethereum, and a bridged token is the
    /// token it was bridged from. They have their own contracts, their own
    /// prices and their own supply figures, which is why TokenProfile treats them
    /// as separate assets -- but a balance sheet that adds their supplies has
    /// counted the same coins twice, and a portfolio that lists them separately
    /// reports two positions where the holder has one exposure.
    ///
    /// Distinct from base_asset: that one says a row is a *pair*, this one says a
    /// row is a *representation*.
    #[prost(message, optional, tag="18")]
    pub wraps: ::core::option::Option<AssetRef>,
}
/// Every asset being tracked.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct AssetRegistry {
    #[prost(message, optional, tag="1")]
    pub meta: ::core::option::Option<super::super::common::v1::RegistryMeta>,
    #[prost(message, repeated, tag="2")]
    pub assets: ::prost::alloc::vec::Vec<Asset>,
}
// Things that have a price and can be traded.
//
// This file answers what an asset *is*: how it is named, what it is known by
// elsewhere, and the class-specific facts that follow from being a company, a
// token, or an item in a game economy. What it is *worth* is kbve.market.v1,
// and what one person did about it is kbve.portfolio.v1. The three meet at
// AssetRef and nowhere else.
//
// Equities are the first class modelled here, because they are the strictest:
// they carry several competing identifier schemes, they trade on venues whose
// ticker namespaces collide, and their price history is rewritten behind them
// by splits and dividends. A model that survives that carries a token or a
// game item without further work.

/// What kind of thing is being traded.
///
/// The class decides how an asset is identified and which venues can quote it,
/// and little else. Everything below is deliberately shaped so that adding a
/// class does not change any message.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum AssetClass {
    Unspecified = 0,
    /// An item in a game economy.
    GameItem = 1,
    Equity = 2,
    Crypto = 3,
    /// A national currency, as the subject of a rate rather than the unit of one.
    Fiat = 4,
    Commodity = 5,
    Index = 6,
    Fund = 7,
}
impl AssetClass {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "ASSET_CLASS_UNSPECIFIED",
            Self::GameItem => "ASSET_CLASS_GAME_ITEM",
            Self::Equity => "ASSET_CLASS_EQUITY",
            Self::Crypto => "ASSET_CLASS_CRYPTO",
            Self::Fiat => "ASSET_CLASS_FIAT",
            Self::Commodity => "ASSET_CLASS_COMMODITY",
            Self::Index => "ASSET_CLASS_INDEX",
            Self::Fund => "ASSET_CLASS_FUND",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "ASSET_CLASS_UNSPECIFIED" => Some(Self::Unspecified),
            "ASSET_CLASS_GAME_ITEM" => Some(Self::GameItem),
            "ASSET_CLASS_EQUITY" => Some(Self::Equity),
            "ASSET_CLASS_CRYPTO" => Some(Self::Crypto),
            "ASSET_CLASS_FIAT" => Some(Self::Fiat),
            "ASSET_CLASS_COMMODITY" => Some(Self::Commodity),
            "ASSET_CLASS_INDEX" => Some(Self::Index),
            "ASSET_CLASS_FUND" => Some(Self::Fund),
            _ => None,
        }
    }
}
/// What form of claim a listed line actually is.
///
/// Every one of these quotes in the same place, on the same screen, in the same
/// currency, and they are not the same instrument. A warrant expiring worthless
/// and a common share are one field apart here and nothing apart in a price
/// feed. It also decides whether the figures in kbve.market.v1.Fundamentals
/// mean anything: a preferred share has a coupon and no earnings claim, so a
/// P/E printed against it is the issuer's P/E attached to the wrong row.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum SecurityType {
    Unspecified = 0,
    Common = 1,
    /// Fixed claim ahead of common, usually without a vote.
    Preferred = 2,
    /// A receipt for shares held abroad. Per-share figures do not carry across
    /// without the ratio -- see EquityProfile below.
    Adr = 3,
    Gdr = 4,
    /// A right to buy at a strike, with an expiry. Has no value after it.
    Warrant = 5,
    Right = 6,
    /// Shares and warrants stapled together, which separate later.
    Unit = 7,
    /// Pays out most of its income by law; the payout ratio screens that work on
    /// ordinary companies read as distress here.
    Reit = 8,
    /// A partnership interest rather than a share. Distributions are not
    /// dividends and are taxed differently.
    LimitedPartnership = 9,
}
impl SecurityType {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "SECURITY_TYPE_UNSPECIFIED",
            Self::Common => "SECURITY_TYPE_COMMON",
            Self::Preferred => "SECURITY_TYPE_PREFERRED",
            Self::Adr => "SECURITY_TYPE_ADR",
            Self::Gdr => "SECURITY_TYPE_GDR",
            Self::Warrant => "SECURITY_TYPE_WARRANT",
            Self::Right => "SECURITY_TYPE_RIGHT",
            Self::Unit => "SECURITY_TYPE_UNIT",
            Self::Reit => "SECURITY_TYPE_REIT",
            Self::LimitedPartnership => "SECURITY_TYPE_LIMITED_PARTNERSHIP",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "SECURITY_TYPE_UNSPECIFIED" => Some(Self::Unspecified),
            "SECURITY_TYPE_COMMON" => Some(Self::Common),
            "SECURITY_TYPE_PREFERRED" => Some(Self::Preferred),
            "SECURITY_TYPE_ADR" => Some(Self::Adr),
            "SECURITY_TYPE_GDR" => Some(Self::Gdr),
            "SECURITY_TYPE_WARRANT" => Some(Self::Warrant),
            "SECURITY_TYPE_RIGHT" => Some(Self::Right),
            "SECURITY_TYPE_UNIT" => Some(Self::Unit),
            "SECURITY_TYPE_REIT" => Some(Self::Reit),
            "SECURITY_TYPE_LIMITED_PARTNERSHIP" => Some(Self::LimitedPartnership),
            _ => None,
        }
    }
}
// @@protoc_insertion_point(module)
