// @generated
// This file is @generated by prost-build.
/// Active tribute drip when a city-state is a vassal.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct Tribute {
    #[prost(uint32, tag="1")]
    pub coin_per_turn: u32,
    #[prost(uint32, tag="2")]
    pub food_per_turn: u32,
    #[prost(uint32, tag="3")]
    pub cadence_turns: u32,
    #[prost(uint32, tag="4")]
    pub next_turn: u32,
}
/// One city-state record. Locked to a hex coordinate so the Rust
/// off-region ticker can reason about distance + chunk membership
/// without dragging Unity transform data across the FFI.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct CityStateRecord {
    #[prost(message, optional, tag="1")]
    pub id: ::core::option::Option<super::super::r#type::v1::Ulid>,
    #[prost(message, optional, tag="2")]
    pub root_hex: ::core::option::Option<super::super::common::v1::Vec2I>,
    #[prost(enumeration="CityStateStatus", tag="3")]
    pub status: i32,
    #[prost(uint32, tag="4")]
    pub mood: u32,
    #[prost(int32, tag="5")]
    pub drift_per_cadence: i32,
    #[prost(message, optional, tag="6")]
    pub tribute: ::core::option::Option<Tribute>,
    /// 0 = independent, 1 = player, 2 = hostile, etc
    #[prost(uint32, tag="7")]
    pub owner_faction: u32,
    #[prost(string, tag="8")]
    pub display_name: ::prost::alloc::string::String,
}
/// Aggregate strategic snapshot. Phase 1 mirrors Unity ECS state into
/// this; Phase 2 lets the Rust uniti tokio task own ticking + persistence
/// for cities outside the active chunk window.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct EmpireSnapshot {
    /// monotonically incremented per emit
    #[prost(uint64, tag="1")]
    pub generation: u64,
    /// WorldClock.TurnIndex at capture
    #[prost(uint32, tag="2")]
    pub turn_index: u32,
    #[prost(message, repeated, tag="3")]
    pub cities: ::prost::alloc::vec::Vec<CityStateRecord>,
}
/// City-state lifecycle status.
///
/// Hostile is zero because a city-state that has never been spoken to is
/// hostile -- the default is a state rather than the absence of one, which is
/// why this enum has no UNSPECIFIED and why the lint rule for it is waived in
/// buf.yaml. Numbers are stable on the wire: saves carry across Unity and Rust,
/// and renumbering to make room for a zero value would rewrite every one of
/// them.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum CityStateStatus {
    Hostile = 0,
    Neutral = 1,
    Allied = 2,
    Vassal = 3,
    Annexed = 4,
    Razed = 5,
}
impl CityStateStatus {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Hostile => "CITY_STATE_STATUS_HOSTILE",
            Self::Neutral => "CITY_STATE_STATUS_NEUTRAL",
            Self::Allied => "CITY_STATE_STATUS_ALLIED",
            Self::Vassal => "CITY_STATE_STATUS_VASSAL",
            Self::Annexed => "CITY_STATE_STATUS_ANNEXED",
            Self::Razed => "CITY_STATE_STATUS_RAZED",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "CITY_STATE_STATUS_HOSTILE" => Some(Self::Hostile),
            "CITY_STATE_STATUS_NEUTRAL" => Some(Self::Neutral),
            "CITY_STATE_STATUS_ALLIED" => Some(Self::Allied),
            "CITY_STATE_STATUS_VASSAL" => Some(Self::Vassal),
            "CITY_STATE_STATUS_ANNEXED" => Some(Self::Annexed),
            "CITY_STATE_STATUS_RAZED" => Some(Self::Razed),
            _ => None,
        }
    }
}
// @@protoc_insertion_point(module)
